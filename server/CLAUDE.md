The SpacetimeDB rules for this module live in [AGENTS.md](AGENTS.md), which the template ships and
every agent here reads. This file exists only to point at it — the two were byte-identical copies,
and one of them would have drifted.
