//! `defs/`'s Rust half (NFR31): everything under here is produced by
//! `tools/defs-build` from `defs/` and committed -- never hand-edited,
//! never regenerated by either build target. `scripts/ci/check-defs-
//! current.sh` is what keeps it honest against `defs/`.

pub mod defs;
