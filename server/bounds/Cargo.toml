[package]
name = "bounds"
version = "0.1.0"
edition = "2024"

# No spacetimedb dependency: see src/lib.rs for why.

[[bin]]
name = "regen-schema-snapshot"
path = "src/bin/regen_schema_snapshot.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
