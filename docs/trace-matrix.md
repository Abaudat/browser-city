# Trace matrix

The invariants named across `requirements.md` and the architecture, and
whether an automated test protects each one today. `covered` rows name the
test function that protects them -- a Rust `#[test]` function, or (story
1.6 on) an `it`/`test` name in `client/tests/unit/**` prefixed `inv_`;
`deferred` rows name the story that will add the subsystem they protect.
`scripts/ci/check-trace-matrix.sh` fails CI if a `covered` row's test does
not exist in either suite, or if an `inv_*` test exists in either suite
with no row here.

| Invariant id | Description | Status | Test | Story |
| --- | --- | --- | --- | --- |
| `inv_identical_seeds_derive_identically` | Two derivations from identical seeded inputs match (NFR25) | covered | `inv_identical_seeds_derive_identically` | — |
| `inv_no_matter_starves` | No matter starves indefinitely | deferred | | matter/needs system |
| `inv_inventory_superset_after_absence` | Inventory is a superset after any absence | deferred | | inventory system |
| `inv_no_owned_item_degrades_during_absence` | No owned item degrades during absence | deferred | | inventory/decay system |
| `inv_budget_never_negative` | Budget never goes negative | deferred | | economy system |
| `inv_collider_within_footprint` | `collider` is contained within `footprint` (FR128); the failure message reports both rectangles, collider and footprint, in sub-cells | covered | `inv_collider_within_footprint` | — |
| `inv_collision_only_within_floor` | No cell on any other floor ever contributes to an entity's collision result (FR117) | covered | `inv_collision_only_within_floor` | — |
| `inv_floor_transition_lands_standable` | No transition cell ever targets a floor or cell where the entity would be inside geometry or out of bounds (FR117) | covered | `inv_floor_transition_lands_standable` | — |
| `inv_world_query_total` | A world query never panics and never wraps, for any i32 coordinate and any floor or layer (FR117) | covered | `inv_world_query_total` | — |
| `inv_cell_ownership_defined` | Every in-bounds cell answers the ownership query, and ids are stable across queries (FR119) | covered | `inv_cell_ownership_defined` | — |
| `inv_depth_order_total_and_stable` | The FR123 sort key is a strict total order (antisymmetric, strict for distinct keys, transitive), so no canopy footprint can ever fail to resolve an order | covered | `inv_depth_order_total_and_stable` | — |
| `inv_drawable_pool_is_a_permutation` | Sorting a drawable pool drops or duplicates nothing -- the output is exactly the input multiset (FR123) | covered | `inv_drawable_pool_is_a_permutation` | — |
| `inv_floor_never_affects_depth_order` | For any two drawables differing only in floor, the comparator's result against a third drawable is identical either way (FR124) | covered | `inv_floor_never_affects_depth_order` | — |
| `inv_multicell_prop_covers_footprint_once` | A multi-cell prop's decomposition yields exactly width*height drawables, anchors covering the footprint exactly once (FR125) | covered | `inv_multicell_prop_covers_footprint_once` | — |
| `inv_move_never_ends_inside_collider` | Starting from any non-penetrating position, any input sequence and any deltaMs (including huge frame spikes) never ends with the player box overlapping a collider (FR137) | covered | `inv_move_never_ends_inside_collider` | — |
| `inv_move_never_tunnels` | A single step with a delta much longer than a thin collider's width never ends on the far side of it | covered | `inv_move_never_tunnels` | — |
| `inv_slide_keeps_tangential_motion` | Moving diagonally into a flat, axis-aligned surface keeps the full tangential component of the motion and zeroes only the normal component; a flush two-cell corner never stops the player on its internal seam | covered | `inv_slide_keeps_tangential_motion` | — |
| `inv_collision_grid_matches_rebuild` | A model-based sequence of random insert/delete/update calls always leaves the grid identical to a from-scratch build of the surviving rows | covered | `inv_collision_grid_matches_rebuild` | — |
| `inv_absent_collider_is_walkable` | An object with no collider contributes nothing: the grid is unchanged by its insert and its delete, and a step in open space is never clamped (FR128) | covered | `inv_absent_collider_is_walkable` | — |
| `inv_step_is_frame_rate_independent` | In open space, one step of N ms equals k steps summing to N ms (within epsilon), and diagonal speed never exceeds axis speed | covered | `inv_step_is_frame_rate_independent` | — |
| `inv_retraction_keyed_on_ownership` | Over a generated terrace of adjacent buildings, any two cells inside the same building's own area give identical visibility for every drawable, and crossing a shared party wall flips retraction between exactly those two buildings' own near-side walls, never a third (FR120) | covered | `inv_retraction_keyed_on_ownership` | — |
| `inv_only_occupied_enclosure_opens` | In a generated terrace of N shops, standing in shop k retracts walls owned by k only (FR120) | covered | `inv_only_occupied_enclosure_opens` | — |
| `inv_floor_culling_exclusive` | Player floor >= 0 means no floor -1-or-below drawable is ever visible, and player floor < 0 means no floor >= 0 drawable is ever visible (FR122) | covered | `inv_floor_culling_exclusive` | — |
| `inv_visibility_never_reorders_pool` | Applying FR120/FR121/FR122 visibility never adds, removes or reorders pool members -- only `sprite.visible`/`sprite.alpha` change | covered | `inv_visibility_never_reorders_pool` | — |
| `inv_footprint_index_matches_rebuild` | A model-based sequence of random insert/delete/update calls always leaves the footprint index identical to a from-scratch build of the surviving rows, allocated chunks included (FR148) | covered | `inv_footprint_index_matches_rebuild` | — |
| `inv_footprint_extent_matches_declared_def` | For any width/height up to `MAX_FOOTPRINT_CELLS` and any anchor (including negative and cross-chunk), one placed row covers exactly the cell set `{x..x+w-1} x {y-h+1..y}`, one entry per object, and removing it clears every one of those cells -- an oracle independent of `footprintOrigin`'s own arithmetic (FR126) | covered | `inv_footprint_extent_matches_declared_def` | — |
| `inv_pick_inverts_screen_position` | For any cell, any floor and any pixel of the screen rect that cell is actually drawn over, picking resolves back to that same cell, through `screen-position.ts`'s own projection constants (FR148) | covered | `inv_pick_inverts_screen_position` | — |
| `inv_reach_is_the_declared_rect` | For any anchor, any declared `interact_at` (negative offsets and rects reaching past the footprint included), any sub-cell count and any two floors, reachability equals the half-open reach rect in continuous world coordinates -- an independent model, so a swapped comparison or a dropped anchor translation fails (FR148) | covered | `inv_reach_is_the_declared_rect` | — |
| `inv_pick_hits_only_what_is_drawn_at_the_point` | For any candidates on one cell -- drawn over the point, drawn elsewhere, or drawing nothing at all, each independently visible -- the pick returns only one actually drawn at that point, chosen by the renderer own sort, and nothing when none is. The broad phase over-registers on purpose, so this is what makes a pick correct (FR148) | covered | `inv_pick_hits_only_what_is_drawn_at_the_point` | — |
| `inv_pick_resolves_topmost_drawn` | For any number of objects covering the clicked point, drawn from any layers in the rank ladder and independently visible or hidden, the pick returns whichever the renderer own sort draws last, and nothing when all are hidden (FR148) | covered | `inv_pick_resolves_topmost_drawn` | — |
| `inv_unreachable_never_emits` | For any player position, a click on an interactable object emits exactly one intent when the player's feet are inside its `interact_at`, and exactly one ignore when they are not -- never both and never neither (FR148) | covered | `inv_unreachable_never_emits` | — |
| `inv_keybindings_injective` | After any sequence of rebinds, no key code is ever bound to two actions, no action is missing, and no reserved code is ever bound (FR149) | covered | `inv_keybindings_injective` | — |
| `inv_keybindings_parse_is_total` | Parsing any value at all as stored bindings never throws and always yields a complete, valid, injective map (FR149) | covered | `inv_keybindings_parse_is_total` | — |
| `inv_keybindings_read_is_total` | Reading any string at all out of the bindings storage key never throws, always yields a complete map, and never writes -- so a parse bug can never destroy saved bindings (FR149) | covered | `inv_keybindings_read_is_total` | — |
| `inv_rebind_survives_reload` | Whatever any sequence of rebinds produces, loading it back yields exactly that map -- `rebind` and `normaliseBindings` cannot disagree about what is bindable, so no binding the player sets can vanish on the next boot (FR149) | covered | `inv_rebind_survives_reload` | — |
| `inv_appearance_deterministic_from_id` | The same citizen id always derives the same five-integer appearance tuple (FR61) | covered | `inv_appearance_deterministic_from_id` | 1.10 |
| `inv_appearance_indices_in_range` | For any u64 citizen id and family, `sim::appearance::generate` never panics and every non-zero index it returns names a real manifest entry of the matching family (FR61) | covered | `inv_appearance_indices_in_range` | 1.10 |
| `inv_kids_parts_only_on_kids_bodies` | A kid family tuple only ever contains kid-family body/eyes/hairstyle/outfit parts, and its accessory is always 0 -- never an adult part (FR61) | covered | `inv_kids_parts_only_on_kids_bodies` | 1.10 |
| `inv_outfit_follows_occupation` | Resolving a profession's uniform override (`resolveUniform`) always yields that profession's declared role_only, adult outfit/accessory by id, for any profession in `defs/professions` that has one (FR62) | covered | `inv_outfit_follows_occupation` | 1.10 |
| `inv_composite_cache_one_texture_per_tuple` | The appearance composite cache returns the same texture instance for the same tuple+override, calls the factory exactly once per unique key, and releases a texture once every reference is gone (FR61) | covered | `inv_composite_cache_one_texture_per_tuple` | 1.10 |
| `inv_composite_cache_bounded` | The appearance composite cache never holds more than its declared engineering cap of entries, evicting least-recently-used ones first | covered | `inv_composite_cache_bounded` | 1.10 |
| `inv_settings_read_is_total` | Reading any string at all out of a settings-storage key never throws and never writes -- the shared idiom every settings group (keybindings, audio, display) is built on | covered | `inv_settings_read_is_total` | 1.11 |
| `inv_connection_notice_tracks_state` | For any sequence of connect/disconnect/error events, at most one connection-notice element ever exists, and once every pending timer has settled it is visible exactly when the last status was not `"connected"` (FR151) | covered | `inv_connection_notice_tracks_state` | 1.11 |
| `inv_collision_overlay_shows_exactly_the_colliders` | For any placed world and any viewport, every object in the window is drawn exactly once, and each of the three collider states comes from the source that can actually know it: a real collider from an independent, from-scratch rebuild of what the live grid reports there, and the zero-area and absent states from the object's own definition -- which is the only source that holds a cell-aligned zero-area collider at all, since the grid rasterises one into no cell (FR165) | covered | `inv_collision_overlay_shows_exactly_the_colliders` | 1.12 |
| `inv_overlay_projection_matches_renderer` | For any cell, floor, tile size, storey height and sub-cell count, the screen rect the overlay draws a cell's sub-cells over is exactly the rect the renderer drew that cell over -- every pixel of it picks back to that same cell through `screen-position.ts`'s own projection, so an overlay can never acquire a constant of its own (FR165) | covered | `inv_overlay_projection_matches_renderer` | 1.12 |
| `inv_sort_overlay_labels_are_the_sort_key` | For any pool, sorting the drawables by the key the sort overlay *printed* reproduces `compareDrawables`' own order over the originals -- the readout is the FR123 key losslessly, not a second derivation of it (FR165) | covered | `inv_sort_overlay_labels_are_the_sort_key` | 1.12 |
| `inv_visible_bounds_cover_every_drawn_cell` | For any renderer size, camera and projection, the viewport window a debug overlay is bounded by contains every cell any pixel of the renderer rect falls on, and is never inverted -- a cell missing from it is a collider that is there and not drawn (FR165) | covered | `inv_visible_bounds_cover_every_drawn_cell` | 1.12 |
| `inv_rule_verdicts_deterministic` | The same facts and rules always give identical violations (FR112, NFR25) | covered | `inv_rule_verdicts_deterministic` | 2.10 |
| `inv_rule_verdicts_independent_of_input_order` | Shuffling fact order or rule row order does not change the sorted output (FR112, NFR25) | covered | `inv_rule_verdicts_independent_of_input_order` | 2.10 |
| `inv_distribution_even_layout_never_violates` | A generated perfectly even 1-per-N layout never violates; clustering all services into one bin always does; a layout that satisfies the minimum spacing while sitting in one corner of the site still violates the coverage bound (AC2, FR112) | covered | `inv_distribution_even_layout_never_violates` | 2.10 |
| `inv_rule_verdicts_invariant_under_tag_relabelling` | Consistently relabelling every tag id, across both the rules and the site, to six arbitrary distinct ids never changes which cells violate, over all five kinds, with areas and a non-vacuous requirement/distribution -- the behavioural half of AC3's "never a bespoke branch" a grep guard cannot give (FR112); extended (story 2.9) to a multi-term, multi-alternative adjacency row, never only the single-term any-direction lowering | covered | `inv_rule_verdicts_invariant_under_tag_relabelling` | 2.10 |
| `inv_adjacency_forbidden_pair_is_reported_exactly` | Over a generated background of cells that cannot interact with the rule, a forbidden pair planted at a random position and orientation is reported exactly once, naming both cells (`Violation.other`); removing the planted neighbour again leaves zero violations (AC2, FR119) | covered | `inv_adjacency_forbidden_pair_is_reported_exactly` | 2.9 |
| `inv_adjacency_direction_respected` | A directional single-term alternative fires only in its own orientation; mirroring the whole site together with the term's own direction never changes the *set* of violating cells, mapped through the same mirror, not merely their count (FR119) | covered | `inv_adjacency_direction_respected` | 2.9 |
| `inv_grammar_rotate_invariant` | Every committed `rotate = true` row gives the same *set* of violating cells over a site mixing wall/threshold/floor/pavement and that same site rotated 90 degrees, mapped through the rotation (AC3, FR119) | covered | `inv_grammar_rotate_invariant` | 2.9 |
| `inv_grammar_verdict_total` | The committed grammar rows never panic over any random composition -- empty, disconnected or with wildly out-of-range coordinates -- and the same facts, inserted in a real shuffled order, always give the same verdict (AC2/AC4, NFR25) | covered | `inv_grammar_verdict_total` | 2.9 |
| `inv_well_formed_room_accepted` | A closed wall ring with a floor interior and a real doorway on a random non-corner wall cell, built from the committed grammar primitives at any size at or above the grammar minimum, is accepted (AC4) | covered | `inv_well_formed_room_accepted` | 2.9 |
| `inv_grammar_accepted_room_is_reachable` | Every room [`inv_well_formed_room_accepted`] accepts, rasterised from that same accepted site and eroded by the real player body, also passes 2.4's `enclosed_regions` and `narrow_passages` checks from an exterior seed (AC4, FR128) | covered | `inv_grammar_accepted_room_is_reachable` | 2.9 |
| `inv_single_mutation_rejected_with_its_reason` | Starting from an accepted room (or, for the four standalone pair cases, a two-cell site at a random position and direction), one mutation from a fixed table is always rejected, and the exact deduplicated set of rows it fires -- evaluated against every committed grammar row -- equals that mutation's own named set (AC4) | covered | `inv_single_mutation_rejected_with_its_reason` | 2.9 |
| `inv_doorway_gap_at_least_body_width_is_one_component_under_erosion` | A ring with a doorway gap at least the player body's own width is always a single connected component once the walkability grid is eroded by the body, wherever the gap sits (FR128) | covered | `inv_doorway_gap_at_least_body_width_is_one_component_under_erosion` | 2.4 |
| `inv_sealed_ring_yields_exactly_one_enclosed_region` | A ring with no gap at all always yields exactly one enclosed region (FR128) | covered | `inv_sealed_ring_yields_exactly_one_enclosed_region` | 2.4 |
| `inv_removing_a_door_never_reduces_enclosed_regions` | Narrowing a ring's own doorway gap -- down to, and including, closing it entirely -- never reduces the number of reported enclosed regions from the same exterior seed (FR128) | covered | `inv_removing_a_door_never_reduces_enclosed_regions` | 2.4 |
| `inv_ring_open_to_any_window_edge_is_never_reported` | A ring's own interior, pushed flush against any one of the window's own four edges with no wall and no margin between them, is never reported by `enclosed_regions` or by `narrow_passages`, for a door narrower than the real player body (FR128) | covered | `inv_ring_open_to_any_window_edge_is_never_reported` | 2.4 |
| `inv_generation_total_never_panics` | Generation is total: for any seed, both passes return a valid plan or a typed error, never a panic (FR110) | covered | `inv_generation_total_never_panics` | 3.2 |
| `inv_generation_all_four_land_uses_present` | Pass 1's coarse grid is fully assigned (no unassigned cell) and every one of the four land uses appears at least once, for any seed (FR110) | covered | `inv_generation_all_four_land_uses_present` | 3.2 |
| `inv_generation_streets_connected_and_not_stranded` | Pass 2's street graph is a single connected component, and every pass-1 region borders a street, for any seed (FR110) | covered | `inv_generation_streets_connected_and_not_stranded` | 3.2 |
| `inv_generation_no_dead_ends_away_from_boundary` | Pass 2 never produces a degree-1 node away from the site boundary, for any seed (FR110, NFR8) | covered | `inv_generation_no_dead_ends_away_from_boundary` | 3.2 |
| `inv_generation_detour_ratio_bounded` | Over the deterministic node-pair sample, BFS network distance never exceeds `max_detour_percent` of Manhattan distance, for any seed -- the contract 3.11's pathfinding estimator relies on (FR110) | covered | `inv_generation_detour_ratio_bounded` | 3.2 |
| `inv_generation_not_a_perfect_grid` | Block width and height each take at least `min_distinct_block_sizes` distinct values, both junction kinds are present, and at least two street classes are present, for any seed (FR110, NFR8) | covered | `inv_generation_not_a_perfect_grid` | 3.2 |
| `inv_generation_exact_tiling` | Every site cell is covered by exactly one block or by at least one street, and no two blocks overlap, for any seed (FR110) | covered | `inv_generation_exact_tiling` | 3.2 |
| `inv_generation_industrial_never_touches_commercial` | No industrial coarse cell is ever adjacent to a commercial one, for any seed (FR110, Artie's direction) | covered | `inv_generation_industrial_never_touches_commercial` | 3.2 |
| `inv_generation_residential_is_the_largest_land_use_by_area` | Residential has more coarse cells than any other single land use, for any seed (FR110, Quentin's direction) | covered | `inv_generation_residential_is_the_largest_land_use_by_area` | 3.2 |
| `inv_generation_p99_detour_ratio_bounded` | The 99th-percentile BFS-network-vs-Manhattan detour ratio, over one city's own sampled pairs, never exceeds `p99_detour_percent`, for any seed (FR110, Tim's direction) | covered | `inv_generation_p99_detour_ratio_bounded` | 3.2 |
| `inv_generation_no_staggered_junctions` | No two junctions on the same street sit under `junction_min_separation_cells` apart unless they coincide, for any seed -- asserted at zero, a refused split rather than a measured ceiling (FR110, Tim's direction) | covered | `inv_generation_no_staggered_junctions` | 3.2 |
| `inv_generation_min_block_depth_is_respected` | Every block is at least `min_block_depth_cells` on both axes, for any seed (FR110) | covered | `inv_generation_min_block_depth_is_respected` | 3.2 |
| `inv_generation_arterials_are_contiguous` | Every arterial line starts at its own near site edge with no gap, and at most one arterial line per city stops short of the far site edge (the T-termination), for any seed (FR110, Artie's direction) | covered | `inv_generation_arterials_are_contiguous` | 3.2 |
| `inv_generation_peripheral_blocks_are_not_degenerate` | Mean block area in the bottom third of the density range is at least 0.7x the top third's, for any seed (NFR8, Tim's/Artie's direction) | covered | `inv_generation_peripheral_blocks_are_not_degenerate` | 3.2 |
| `inv_generation_institutional_pockets_are_small` | At least `institutional_min_pockets` mutually non-adjacent (edge or corner) institutional components per site, none over `institutional_max_pocket_share_percent` of the site's own coarse-cell count, for any seed (FR110, Artie's direction) | covered | `inv_generation_institutional_pockets_are_small` | 3.2 |
| `inv_generation_every_plot_fronts_a_street` | Every non-open plot shares at least `frontage_min_cells` of edge length with a street-abutting side of its own block, for any seed (story 3.3 AC1, FR110) | covered | `inv_generation_every_plot_fronts_a_street` | 3.3 |
| `inv_generation_plots_tile_their_block` | Every plot is inside its own block, no two plots overlap, and a block's own area minus its plots' summed area (the explicit remainder) is never negative, for any seed (story 3.3 AC1, FR110) | covered | `inv_generation_plots_tile_their_block` | 3.3 |
| `inv_generation_no_envelope_below_its_class_minimum` | Every placed envelope's footprint is at least its own land use's minimum (interior plus the wall ring) on both axes, for any seed (story 3.3 AC2, FR110, FR115) | covered | `inv_generation_no_envelope_below_its_class_minimum` | 3.3 |
| `inv_generation_envelope_inside_its_own_plot` | Every placed envelope's footprint is inside its own plot's bounds, and its own front matches that plot's front, for any seed (story 3.3 AC2, FR110) | covered | `inv_generation_envelope_inside_its_own_plot` | 3.3 |
| `inv_generation_envelope_sizes_are_varied` | A district shows at least `min_distinct_sizes` distinct (along_face, depth) envelope footprint pairs, for any seed -- a city of identical boxes must not satisfy the mean band alone (story 3.3 AC3, NFR8) | covered | `inv_generation_envelope_sizes_are_varied` | 3.3 |
| `inv_generation_building_count_within_tolerance` | At the committed config, generation succeeds (`envelopes::run` returns `Ok`) for any seed (story 3.3 AC4, FR110) | covered | `inv_generation_building_count_within_tolerance` | 3.3 |
| `inv_generation_envelope_rejection_rate_bounded` | The percent of attempted (non-open) plots rejected by the envelope pass never exceeds `max_rejected_plot_percent`, for any seed (story 3.3 AC4, Quentin's direction) | covered | `inv_generation_envelope_rejection_rate_bounded` | 3.3 |
| `inv_generation_all_four_passes_never_panic` | `inv_generation_total_never_panics` extended to all four implemented passes: for any seed, generation reaches a plan or a typed error, never a panic, and pass 3 always produces at least one plot (story 3.3, FR110) | covered | `inv_generation_all_four_passes_never_panic` | 3.3 |
| `inv_generation_envelope_mean_size_matches_the_committed_band` | Pooled over the fixed seed range 0..256, the mean placed-envelope footprint width and depth each sit within their own committed +-tolerance band (story 3.3 AC3) | covered | `inv_generation_envelope_mean_size_matches_the_committed_band` | 3.3 |

## Coverage scale (NFR29)

Rows above track whether an invariant is exercised at all; this tracks
whether it is exercised at the scale NFR29 asks for. Not part of the
id/constant/test symmetry `check-trace-matrix.sh` enforces above (there is
no `INV_` constant for a case count) -- checked by eye until there is a
citizen simulation to run it against.

| Requirement | Status | Blocked on |
| --- | --- | --- |
| Property suite runs thousands of simulated citizen-weeks (NFR29) | deferred | citizen simulation |

## Round trip and client/server boundary

Not part of the `inv_*`/`INV_*` id symmetry above (these guard requirements
that span the client/server boundary or the CI graph itself, not a `sim`
invariant), but not just checked by eye either: `check-trace-matrix.sh`
asserts that the path named in every `covered` row's Guard column exists.
A guard renamed or deleted without updating this table fails CI.

| Requirement | Status | Guard |
| --- | --- | --- |
| Browser-exclusive, no install/plugin/download gate (NFR5) | covered | `client/tests/e2e/round-trip.spec.ts` -- runs the client in stock headless Chromium with no flag, plugin or install step |
| No code shared between `server/` and `client/` (NFR30) | covered | `scripts/ci/check-no-shared-code.sh` |
| Sim purity, reducers as the only table-touching layer (NFR28) | covered | `scripts/ci/check-sim-purity.sh` (unchanged by this story) |
| TeV per reducer class instrumented from day one (NFR17) | deferred | first real reducer class -- nothing to measure yet (story 1.1) |

## Schema permanence

Story 1.2: the permanent decisions NFR33-NFR37 forbid ever getting wrong
in a second commit. Same Guard-path discipline as the section above --
`check-trace-matrix.sh` asserts every `covered` row's path exists.

| Requirement | Status | Guard |
| --- | --- | --- |
| Every table has exactly one primary key; no Rust enum in module source; tables stay under the column ceiling or carry a waiver (NFR33, NFR35, NFR36) | covered | `server/bounds/tests/schema_shape.rs` |
| Every scheduled table carries `scheduled_id`/`scheduled_at` and names a real, scheduler-only reducer (NFR34) | covered | `server/bounds/tests/schema_shape.rs` -- `every_scheduled_table_has_the_required_columns_and_names_a_real_reducer` |
| The schema snapshot is never stale against source | covered | `server/bounds/tests/schema_snapshot_current.rs` |
| A moved primary key, a moved unique constraint, a changed scheduled status, a removed/retyped/reordered column, or a column inserted ahead of an existing one fails the build (NFR33) | covered | `scripts/ci/check-schema-additive.sh` |
| Appending a column with a default publishes against a live world; appending one without a default is rejected, not silently accepted, and the rejection is the automigration one -- not a build or connection failure passing for it (NFR33) | covered | `scripts/ci/check-live-migration.sh` |
| The character&lt;-&gt;identity mapping is one-character-to-N-identities, `identity` unique and `character_id` a plain index (FR142, D5) | covered | `server/schema.snapshot.json` (`character_identity`'s row: `identity`'s `unique` and `character_id`'s `indexed` fields, pinned and diffed by the two guards above) |
| A committed `sim::codes` line (a code number or a name) never changes or disappears once merged; only new lines may be appended (NFR36) | covered | `scripts/ci/check-codes-append-only.sh` |
| Re-seeding the extensible-set companion tables is idempotent (`reseed_codes`) (NFR36, NFR38) | covered | `scripts/ci/check-live-migration.sh` |
| An operator-only reducer rejects any caller that is not the module owner (`reseed_codes`, `tables::ops::require_owner`) | covered | `scripts/ci/check-live-migration.sh` |

## Definitions

Story 2.1: `defs/` is the only source of truth; both targets consume
generated output, never each other, under one `defs_version` (NFR31).
Same Guard-path discipline as the sections above.

| Requirement | Status | Guard |
| --- | --- | --- |
| The generator emits a Rust include and a client JSON asset from `defs/`, and neither is ever hand-edited | covered | `scripts/ci/check-defs-current.sh` |
| A malformed definition fails the build naming the offending file and line, and no partial output is emitted | covered | `tools/defs-build/tests/failure_fixtures.rs` |
| An id or a key, once merged, is never renumbered, reused or retired | covered | `scripts/ci/check-defs-ids-append-only.sh` |
| The single `defs_version` changes whenever anything under `defs/` changes, and never independently | covered | `scripts/ci/check-defs-version-bump.sh` |
| Both generated artefacts carry the exact same `defs_version`, asserted directly rather than inferred, and the built client asset ships it too | covered | `scripts/ci/check-defs-version-agrees.sh`, the `client-build` job's own build-asset assertion |
| The server and the client independent parsers agree on every field, and on a shared table of malformed input both must reject (NFR30) | covered | `server/sim/tests/defs_dump.rs`, `client/tests/unit/defs/dump-golden.test.ts`, `tools/defs-build/tests/shared_malformed_cases.rs`, `client/tests/unit/defs/malformed.test.ts` |
| The prop atlas pages fold into `defs_version` -- no separate atlas manifest under `defs/`, because every packer input (an object's own `sprite` rect) already lives in `defs/objects/` (story 2.6) | covered | `tools/defs-build/tests/atlas_pixel_roundtrip.rs`; `scripts/ci/check-defs-current.sh` (extended to the whole `client/public/atlas/` directory) |
| The character-part atlases fold into `defs_version` -- no separate manifest; every input (a body/eyes/hairstyle/outfit/accessory's own `sheet`) already lives in `defs/appearance/` (story 2.7) | covered | `tools/defs-build/tests/atlas_pixel_roundtrip.rs`; `scripts/ci/check-defs-current.sh` |
| The audio manifest folds into `defs_version` | deferred | the story that adds the audio pipeline -- the input does not exist yet |
| An object `collider` (sub-cells, FR128) rejects zero/negative area and a rect that does not fit inside `width*height*COLLIDER_SUBCELLS_PER_CELL`; `COLLIDER_SUBCELLS_PER_CELL` is generated once into both artefacts; a containment failure's message reports both rectangles, collider and footprint, in sub-cells, in a fixed order (story 1.8, tightened story 2.4) | covered | `tools/defs-build/tests/failure_fixtures.rs` -- `a_zero_area_collider_is_named`, `a_collider_outside_its_footprint_is_named`; `tools/defs-build/tests/shared_malformed_cases.rs` |
| **AC1**: every prop either declares a `collider` or carries the `underfoot` tag (`defs/tags/city.toml`), never neither and never both -- a colliderless prop nobody tagged is rejected by name, naming the `underfoot` tag as the fix; not a hard-coded allow-list of object keys (story 2.4, FR128) | covered | `tools/defs-build/src/validate.rs` -- `a_colliderless_object_not_tagged_underfoot_is_rejected`, `an_object_tagged_underfoot_with_a_collider_is_rejected`, `underfoot_allow_listed_props_with_no_collider_pass`; `tools/defs-build/tests/failure_fixtures.rs` -- `a_colliderless_prop_not_tagged_underfoot_is_named`, `a_manhole_absent_from_the_underfoot_tag_is_named`, `an_object_tagged_underfoot_with_a_collider_is_named`; `tools/defs-build/tests/shared_malformed_cases.rs`; `client/src/defs/parse.ts` -- `checkObjectWalkabilityTag`; `client/tests/unit/defs/parse.test.ts` -- the story 2.4 block |
| **AC3**: a collider outside its own sprite bounds is refused -- a corollary of the sprite/footprint agreement (FR126) and the collider/footprint containment rule (FR128) together, made explicit rather than duplicated as a third check (story 2.4) | covered | `tools/defs-build/src/validate.rs` -- `a_collider_outside_its_sprite_is_rejected_as_a_footprint_containment_failure`; `client/tests/unit/defs/parse.test.ts` -- "rejects a collider outside its own sprite bounds as a footprint containment failure (story 2.4 AC3)" |
| **AC6**: invariant checks run before any artefact write -- validated everything first, emitted only on `Ok`, and a failed build leaves whatever the output directory already held byte-identical (story 2.4) | covered | `tools/defs-build/tests/failure_fixtures.rs` -- `every_invalid_fixture_leaves_pre_existing_output_untouched` (extended automatically to every new invalid fixture category, `underfoot`'s included, since it reads `tests/fixtures/invalid/` itself) |
| An `[[object]]` carries `name`, `layer` (resolved against the codes golden, an unknown or deprecated name refused) and `sprite` (fits its own sheet, agrees with the footprint exactly) (story 2.2) | covered | `tools/defs-build/src/validate.rs` -- `an_object_with_an_empty_name_is_rejected`, `an_object_naming_an_unknown_layer_is_rejected`, `an_object_naming_a_deprecated_layer_is_rejected`, `a_sprite_rect_outside_its_sheet_is_rejected`, `a_sprite_width_not_matching_the_footprint_width_is_rejected`, `a_sprite_height_not_a_whole_multiple_of_tile_size_is_rejected`, `a_sprite_height_shorter_than_the_footprint_height_is_rejected`; `tools/defs-build/tests/failure_fixtures.rs` |
| Every object carries a required, JSON-only `atlas` rect (page index, whole sprite in page pixels, gutter excluded) resolved by the packer, never optional and never a client-side fallback to `sprite.sheet` (story 2.6) | covered | `tools/defs-build/src/emit.rs` -- `emit_json_renders_atlas_pages_and_each_objects_atlas_rect`, `emit_json_panics_if_an_object_is_missing_its_atlas_rect`, `emit_rust_never_carries_an_atlas_field_or_table`; `client/src/defs/parse.ts` -- `checkObjectAtlasPage`; `client/tests/unit/defs/parse.test.ts` |
| **AC1**: only the used sprite subset is packed, into 2048x2048-capped pages; a sprite bigger than a page after padding is refused by name (story 2.6) | covered | `tools/defs-build/src/atlas/pack.rs` -- `a_sprite_bigger_than_a_page_after_padding_is_refused_by_name`, `placed_rects_never_overlap_and_always_fit_the_page_with_no_rotation` (proptest); `tools/defs-build/tests/atlas_used_subset_and_determinism.rs` -- `only_the_used_subset_is_read_a_corrupt_unreferenced_sheet_never_breaks_the_build` |
| **AC2**: pages are grouped by the tileset's own theme-sorter organisation, derived from the sheet path, never a hand-written list or a per-object field; a derived theme resolves to its actual page group through `defs/atlas/page-groups.toml`'s own table, a theme with no row failing the build by name (story 2.6, Artie's direction cycle 1) | covered | `tools/defs-build/src/atlas/theme.rs` -- `derives_the_group_for_every_exterior_theme_sorter_dir_this_tileset_ships`, `derives_the_group_for_every_interior_theme_sorter_singles_dir_this_tileset_ships`, `resolve_page_group_maps_a_known_theme`, `resolve_page_group_fails_naming_an_unmapped_theme`; `tools/defs-build/src/atlas/pack.rs` -- `a_group_needing_a_third_page_fails_naming_the_group_and_page_count` |
| A spawn screen's own object set resolves against few pages: every street-kit theme the real tree uses today (terrain, city props, generic/floor-modular buildings and the single-prop-borrowing themes) maps to one shared `"street"` page group, so today's real 8-object street kit binds exactly 1 page, not one per theme folder (story 2.6, Artie's direction cycle 1) | covered | `defs/atlas/page-groups.toml`; `tools/defs-build/src/atlas/build.rs` -- `two_themes_mapped_to_the_same_page_group_share_pages`; the real `cargo run --bin defs-build` output (`client/public/atlas/`, one `street-*.png` page) |
| **AC3**: an unchanged page's filename is unchanged between builds -- content-hashed over pixels, never PNG bytes; changing one group's pixels never renames another group's page (story 2.6) | covered | `tools/defs-build/src/atlas/build.rs` -- `page_filenames_are_content_hashed_and_deterministic`, `changing_one_groups_pixels_never_renames_another_groups_page`; `tools/defs-build/src/atlas/pack.rs` -- `packing_is_deterministic_regardless_of_input_order`, `packing_is_deterministic_under_shuffling` (proptest); `tools/defs-build/tests/atlas_used_subset_and_determinism.rs` -- `packing_two_groups_is_byte_identical_regardless_of_file_order` |
| **AC4**: the packer emits the atlas pages and the `object_def` rects together as one `defs-build` run; both are covered by the same `defs_version` | covered | `tools/defs-build/src/bin/defs-build.rs`, `tools/defs-build/src/lib.rs::build` (one call produces both); `scripts/ci/check-defs-current.sh` |
| **AC5** (NFR12): a single page group never spans more than `ATLAS_MAX_PAGES_PER_GROUP` pages; a scene is the shared `street` group plus at most one themed group (a player is never on the street and inside a themed interior at once), so the shared group's own page count plus the worst other group's own page count never spans more than `ATLAS_MAX_BOUND_PAGES` (8) -- both asserted at build time by name, not hoped for | covered | `tools/defs-build/src/atlas/pack.rs` -- `a_group_needing_a_third_page_fails_naming_the_group_and_page_count`; `tools/defs-build/src/atlas/build.rs` -- `nine_single_page_themed_groups_beside_a_one_page_shared_group_pass`, `a_two_page_shared_group_and_a_seven_page_themed_group_fails_naming_both` |
| Every packed rect carries a permanent extruded (edge-repeated, never transparent) 1px border -- prevents bleed at a fractional camera position; pixels round-trip byte-for-byte from source sheet to packed page, over every real object, never a sample (Artie's direction) | covered | `tools/defs-build/src/atlas/image.rs` -- `composite_with_extrusion_copies_pixels_exactly_and_repeats_edges`; `tools/defs-build/src/atlas/build.rs` -- `atlas_pixel_round_trips_through_the_page_exactly`; `tools/defs-build/tests/atlas_pixel_roundtrip.rs` -- `every_real_objects_atlas_pixels_match_its_source_sprite_rect_exactly` |
| **AC1** (story 2.7): all five character parts share one family layout, asserted by the build, not assumed -- a mismatched part sheet fails naming the part's own kind, key and sheet; one failing case per kind, plus a real mismatched-PNG fixture proving the case a header-only size check cannot see | covered | `tools/defs-build/src/atlas/character.rs` -- `a_declared_cell_reaching_past_the_decoded_sheet_is_named`, `every_non_body_kind_still_names_itself_on_a_decode_bounds_failure`, `a_fully_transparent_strip_is_named`, `a_body_with_an_empty_cell_is_named`; `tools/defs-build/tests/failure_fixtures.rs` -- `a_real_body_sheet_too_small_for_its_own_declared_layout_grid_is_named` |
| **AC1**, over every real part in `defs/appearance/`, never a sample: every used `(row, direction, frame)` cell's packed pixels match its source cell byte for byte, plus a 1px extruded-border spot check (story 2.7) | covered | `tools/defs-build/tests/atlas_pixel_roundtrip.rs` -- `every_real_objects_atlas_pixels_match_its_source_sprite_rect_exactly` |
| Packing a character part is deterministic and never overlaps under a real shuffle of the part list (story 2.7) | covered | `tools/defs-build/src/atlas/character.rs` -- `packing_character_parts_is_deterministic_under_shuffling` (proptest) |
| **AC2**: the Epic 1 shortcut (raw vendor part sheets fetched at runtime) is removed, not bypassed -- the client loads packed character atlases instead, through the same lazily-fetched-page idiom the object atlas uses (story 2.7) | covered | `client/src/render/appearance/character-part-pages.ts`; `scripts/ci/check-no-raw-part-sheets.sh`; `client/tests/e2e/appearance.spec.ts` -- "the client reads no vendor character sheets, only the character atlas pages the mounted crowd actually references" |
| **AC3** (NFR12): five character layers plus tile/prop atlases stay within budget -- `shared + worst themed + CHARACTER_COMPOSITE_PAGES <= ATLAS_MAX_BOUND_PAGES`, character-part groups excluded from the worst-themed term (CPU-only, never bound); a failure names all three terms and the total (story 2.7) | covered | `tools/defs-build/src/atlas/build.rs` -- `a_scene_passes_exactly_at_the_bound_pages_limit`, `character_composite_pages_pushing_a_scene_over_the_limit_fails_by_name`, `a_character_part_group_is_excluded_from_the_worst_other_group_term` |
| A crowd of N distinct looks and a crowd of N identical looks bind the exact same number of distinct GPU texture sources -- "different people cost about as much as identical ones" -- for N >= 32 (story 2.7) | covered | `client/tests/e2e/appearance.spec.ts` -- "different people cost about as much as identical ones..." (mounts the real crowd, `?identicalCrowd=1` collapses it to one tuple per family, `allBoundTextureSources` -- every distinct `TextureSource` reachable from the display list, unfiltered -- and `distinctBoundAtlasPages` both compared for exact equality) |
| Footprints are capped at `MAX_FOOTPRINT_CELLS` (approximately 8x8), checked on width and height independently; the error names the object and directs the author to compose the structure from multiple objects (FR127) | covered | `tools/defs-build/src/validate.rs` -- `a_footprint_of_exactly_the_cap_on_both_axes_is_accepted`, `a_footprint_one_wider_than_the_cap_is_rejected`, `a_footprint_one_taller_than_the_cap_is_rejected`, `a_footprint_exceeding_the_cap_on_both_axes_is_rejected`, `a_footprint_width_of_zero_is_rejected`; `client/src/defs/parse.ts` -- `checkObjectFootprintCap` |
| The footprint cap never exceeds the chunk size, because the region-subscription halo a multi-cell object's overhang relies on depends on `MAX_FOOTPRINT_CELLS` being a real constant (FR127) | covered | `server/sim/src/world/chunk.rs`'s compile-time assert (`const _: () = assert!(...)`) |
| There is no separate `walkable` field anywhere in the schema (FR128): `deny_unknown_fields` refuses it, and a grep guard refuses the identifier itself under `defs/`, the generated Rust and the generated JSON | covered | `tools/defs-build/tests/failure_fixtures.rs` -- `a_walkable_flag_is_named`; `scripts/ci/check-no-walkable-field.sh` |
| A multi-cell prop's anchor is its footprint's smallest x, largest y cell; `collider`/`interact_at` and the client's decomposition, footprint index and collision grid all agree on that convention, proven against an asymmetric multi-row footprint (FR125, FR126) | covered | `client/tests/unit/render/decompose.test.ts` -- `an asymmetric multi-row footprint anchors at its south-west corner, not its top-left`; `client/tests/unit/world/footprint-index.test.ts` -- `an asymmetric multi-row footprint covers cells north of its anchor, never south`; `client/tests/unit/world/collision-grid.test.ts` -- `an asymmetric multi-row footprint's collider rasterises relative to its south-west anchor, not its top-left` |
| A new constraint is a row in `defs/rules/`, never an engine code change (AC1); a real TOML row, run through the real parse/validate path, renders the exact `RuleKind` literal the generated artefact would carry, and the *committed* artefact itself rejects the example the ticket names (story 2.10) | covered | `tools/defs-build/tests/rule_emit_acceptance.rs` -- `ac1_no_cafe_above_floor_2_renders_the_exact_rule_kind_literal`; `server/sim/tests/rule_examples.rs`; `server/sim/tests/rule-examples/lighting_ground_floor_only/` |
| The engine supports exactly five closed constraint kinds -- placement, distribution, coherence, adjacency, requirement -- each expressible against real content, and a sixth kind is a compile error until `RuleKind`'s exhaustive `match` is updated on purpose (AC2/AC3, story 2.10) | covered | `server/sim/src/rules/mod.rs` -- one satisfied/violated/boundary test per kind; `defs/rules/city.toml` -- one row per kind |
| Distribution's "evenly spread" (AC2) is a ratio, a minimum spacing and a maximum coverage distance (`max_distance`), not spacing alone -- a `per` cell with no `subject` cell within `max_distance` violates, bucketed like spacing, never an all-pairs scan; `max_distance == 0` is refused at build time | covered | `server/sim/src/rules/mod.rs` -- `distribution_coverage_violates_when_a_per_cell_has_no_nearby_subject`, `distribution_coverage_boundary_exactly_at_max_distance_passes_one_past_fails`, `distribution_coverage_zero_subjects_leaves_every_per_cell_uncovered`, `distribution_clustered_in_one_corner_fails_coverage_even_though_spacing_is_satisfied`; `tools/defs-build/tests/failure_fixtures.rs` -- `a_distribution_max_distance_of_zero_is_rejected` |
| The rule engine never hardcodes a branch keyed on a content key; a malformed rule row (unknown kind, a field belonging to another kind, a dangling tag reference, a non-positive distribution ratio/tolerance/max_distance, an inverted placement floor range, a duplicate rule id) fails the build naming the file and line, and `evaluate` behaves identically under any consistent tag-id relabelling (the behavioural half a grep guard cannot give) (story 2.10) | covered | `scripts/ci/check-rule-engine-no-content-keys.sh`, self-tested by `scripts/ci/tests/test-check-rule-engine-no-content-keys.sh`; `tools/defs-build/tests/failure_fixtures.rs`; `inv_rule_verdicts_invariant_under_tag_relabelling` |
| Requirement never double-counts a container cell that fails in two areas at once, and a container cell outside every real area is itself a violation (never silently unchecked) (story 2.10) | covered | `server/sim/src/rules/mod.rs` -- `requirement_deduplicates_a_container_cell_failing_in_two_areas_at_once`, `requirement_container_outside_any_area_is_itself_a_violation` |
| Tags (`defs/tags/*.toml`) are the rule engine's only vocabulary; an object's `tags` field and a rule row's own tag references resolve against the declared set on both sides identically, and reach `defs.json` as a required field on both the document root and every object -- rule rows never do, the client never evaluates a rule (story 2.10) | covered | `tools/defs-build/src/validate.rs`, `client/src/defs/parse.ts` -- `checkObjectTags`; `client/tests/unit/defs/parse.test.ts` -- the story 2.10 block |
| There is no separate rule-set version: `defs_version` already hashes `defs/rules/` and `defs/tags/` like every other tracked path, and is the rule-set version FR108/FR109 refer to (story 2.10) | covered | `scripts/ci/check-defs-version-bump.sh`, `scripts/ci/check-defs-version-agrees.sh` (unchanged, already unconditional over every tracked path under `defs/`) |
| **AC1**: a tile's role (ground, pavement, road, wall, floor, threshold, fixture) is data in `defs/` -- a role tag's own `role = { layers = [...] }` table, never a second kind or a code enum; every object carries exactly one role tag, and its own layer must be among that role's `layers` -- zero roles, two roles, an object's layer outside them, and a role naming an unknown/deprecated layer all fail the build by name (story 2.9, FR119) | covered | `tools/defs-build/src/validate.rs` -- `check_tag_role_layers`, `check_object_roles`; `tools/defs-build/tests/failure_fixtures.rs` -- `an_object_with_no_role_tag_is_rejected`, `an_object_with_two_role_tags_is_rejected`, `an_objects_layer_outside_its_own_roles_allowed_layers_is_rejected`, `a_role_naming_an_unknown_layer_is_rejected`; `client/src/defs/parse.ts` -- `checkObjectRole`; `client/tests/unit/defs/parse.test.ts`; `fixtures/defs-malformed-cases.v1.json` -- `object-role-count-zero`, `object-role-count-two` |
| **AC2**: adjacency reports the violating pair -- `Violation.other` names the matched neighbour cell for a `Forbid` row, and the rule id names the rule that forbids it; a `Forbid` row's alternatives are always a single `present: true` term so the pair is unambiguous, refused at build time otherwise, and two `Forbid` rows for the same pair with subjects swapped are refused too (story 2.9, FR119) | covered | `server/sim/src/rules/mod.rs` -- `adjacency_forbid_violation_names_the_matched_neighbour_as_other`, `adjacency_forbid_reports_one_violation_per_matching_alternative`; `tools/defs-build/tests/failure_fixtures.rs` -- `a_forbid_row_with_a_multi_term_alternative_is_rejected`, `two_forbid_rows_for_the_same_pair_with_subjects_swapped_are_rejected`, `an_adjacency_alternative_with_a_dead_contradictory_term_pair_is_rejected`; `inv_adjacency_forbidden_pair_is_reported_exactly` |
| **AC3**: a wall corner and a formed doorway are grammar primitives in data, not special cases in code -- `[[adjacency]]`'s `alternatives`/`rotate` author a neighbourhood pattern, and `tools/defs-build` lowers both that form and the terse `b` form into the engine's one `alternatives` shape; `wall_run` (carried by both `wall` and `threshold`) is what the closure row counts, so a doorway's own jambs and a T-junction/four-way crossing are never mistaken for a free-standing stub (story 2.9, FR119) | covered | `defs/rules/grammar.toml` -- `doorway_formed_between_walls`, `wall_is_part_of_a_straight_run_or_a_corner`; `server/sim/tests/rule-examples/doorway_formed_between_walls/`, `server/sim/tests/rule-examples/wall_is_part_of_a_straight_run_or_a_corner/`; `inv_grammar_rotate_invariant` |
| **AC4**: an agent composing a room or building from the grammar primitives either satisfies every row or is rejected with a named reason -- room completeness (`room_has_a_door`) and building completeness (`building_has_an_entrance`) are `[[requirement]]` rows over real areas, an `entrance` must itself open onto pavement (`entrance_opens_onto_pavement`) so the claim it makes is never a lie, and `evaluate` runs over the real committed `RULES`, never a second validator (story 2.9, FR119) | covered | `server/sim/tests/rule_examples.rs`, evaluated over the well-formed room shared as each of `room_has_a_door`'s, `building_has_an_entrance`'s, `entrance_opens_onto_pavement`'s and `doorway_formed_between_walls`'s own `pass.grid`, plus one broken-variant `fail.grid` per row; `inv_well_formed_room_accepted`; `inv_grammar_accepted_room_is_reachable`; `inv_single_mutation_rejected_with_its_reason`; `inv_grammar_verdict_total` |
| Scope named, not built, in this story: a kerb/verge edge variant between `road` and `ground` (the art exists in `2_City_Terrains`, cutting and packing it does not fit this story's budget -- `road_never_touches_ground` refuses the bare seam until it does), "no void cell inside a composed area" (no rule kind asks "does this cell carry any tag at all"), and real `floor`/`threshold` objects (story 2.14, issue #300, gives the atlas packer a path for the `Room_Builder_subfiles` sheets they need) (story 2.9) | deferred | `defs/rules/grammar.toml`'s own header comment; `defs/objects/terrain.toml`'s own header comment name each gap |
| Rule evaluation over a full generated city stays within the generation budget | deferred | the Epic 3 generator story that first calls `sim::rules::evaluate` against a real, full-sized candidate city -- no generator exists yet |
| **AC1**: `evaluate` accepts only a `RuleSet`, whose field is private outside its own module -- the only two constructors are `RuleSet::committed` (wraps `generated::defs::RULES`) and, gated to `test`/`test-fixtures`, `RuleSet::for_test`; naming `test-fixtures` on any `server/sim/Cargo.toml` line but its own `[features]` declaration and self dev-dependency, enabling it from any manifest but that one and `server/bounds/Cargo.toml`, the *resolved* feature graph for `browser_city` ever turning it on, or `for_test`/`RuleKind`/`RULES` (the bare words) appearing outside `server/sim/src/rules/`, all fail the build by name (story 2.11, FR112) | covered | `scripts/ci/check-rule-source.sh`, self-tested by `scripts/ci/tests/test-check-rule-source.sh`; `server/sim/src/rules/source.rs`'s own `compile_fail` doctest |
| **AC1/AC4**: the harness refuses (`ValidationError::RuleSourceMismatch`), never silently validates or reports an empty result, when a candidate is stamped with a `defs_version` other than this build's own committed one; the identical candidate stamped with the committed version validates normally (story 2.11, FR112) | covered | `server/sim/tests/validation.rs` -- `a_candidate_stamped_with_a_foreign_defs_version_is_refused_not_silently_validated`, `the_same_site_stamped_with_the_committed_version_validates_normally` |
| **AC2**: the harness (`sim::validation::validate`) reports no defects at all over a correct block (story 2.11, FR112) | covered | `server/sim/tests/validation.rs` -- `correct_block_validates_with_no_defects` |
| **AC3**: each of a doorway too narrow, a sealed room with no door, a coherence break and a placement break is reported individually, naming the rule (by key) or the walkability check and the full location; combined into one candidate, every defect from every break is still reported, including the same rule violated at several distinct cells across two different breaks at once -- the harness never stops at the first (story 2.11, FR112) | covered | `server/sim/tests/validation.rs` -- `a_doorway_too_narrow_is_reported_and_only_it_changes_the_correct_block`, `a_sealed_wall_ring_with_no_door_is_reported`, `a_counter_sharing_a_stairwells_area_is_reported`, `a_lamppost_above_the_ground_floor_is_reported`, `all_four_breaks_combined_report_every_defect_with_no_short_circuit`, `a_rule_defect_renders_the_rule_key_and_the_full_cell_location`, `a_forbid_adjacency_defect_renders_the_other_cell_too` |
| **AC4**: a test deliberately points two consumers at different rule sources (a manifest enabling `test-fixtures`, a `for_test`/`RuleKind`/`RULES` use, a resolved feature graph turning `test-fixtures` on for the published module, each outside the one allowed module) and asserts a real, failing build, never a doc line (story 2.11, FR112) | covered | `scripts/ci/tests/test-check-rule-source.sh` |
| Performance: `sim::validation::validate` (including `PlacedSite::build`) is in scope for the rule-evaluation-over-a-full-generated-city budget above, not a separate concern the generator story could skip | deferred | the Epic 3 generator story that first calls `sim::validation::validate` against a real, full-sized candidate city -- no generator exists yet; `server/sim/src/validation.rs`'s own `PlacedSite::build` is already bounded by placed cells and areas, never by cells times areas, ahead of that budget existing |
| The generator half of FR112's "one source" -- Epic 3's generator itself calling `RuleSet::committed()` | deferred | story 3.2's own two passes (`sim::generation::land_use`/`streets`) hand down an abstract plan, never a placed `ObjectDef`, so `sim::validation` has nothing to judge yet and this row stays deferred -- re-pointed at the first pass that actually places objects (plot subdivision/building envelope, 3.3 on); `sim::rules::RuleSet::committed` is the binding it must use, story 2.11's own harness half is `covered` above |
| A closed room with a real door -- `floor`/`threshold` objects real content can place -- joins `correct_block_validates_with_no_defects` as content for the correct block (story 2.11, FR112) | deferred | story 2.14, issue #300, gives the atlas packer a path for the `floor`/`threshold` sheets first; `server/sim/tests/validation.rs`'s own module doc names the gap today |
| The AC3 example breaks named in the story text but not built here -- "a skyscraper among villas" (coherence) and "a cafe on the tenth floor" (placement) are content for Derek's later stories; swapping them in for `no_counter_in_a_stairwell`/`lighting_ground_floor_only` in `server/sim/tests/validation.rs` is a fixture edit, not a harness change (story 2.11, FR112) | deferred | Derek's later content stories; today's `no_counter_in_a_stairwell` (coherence) and `lighting_ground_floor_only` (placement) breaks already exercise the same two kinds over real committed rows |
| **AC1**: a footprint proposal is derived from alpha coverage of the sprite's own lower band only, never a whole-sprite pre-scan (no pixel above the band is ever read, structurally: every loop is bounded by the band's own top row); it is explicitly a starting point, never authority -- the band having no opaque pixel at all, and the sprite's own bottom *tile* having no run of opaque rows touching it at all, are each a distinct named refusal, never a silent `0x0`/`1x1` or a panic. Depth is the longest contiguous run of opaque rows that touches the bottom tile, not necessarily one starting at the sprite's own last pixel row: a gap smaller than one tile (about a fifth of this tileset's own real singles have one) is tolerated, monotonically -- a shorter run found deeper inside the gap can never beat a longer, better-connected run behind it, which a naive "anchor to the single lowest opaque row" rule does not guarantee. Proven over real varied sizes and a `prop_oneof!` of named shapes (each one *is* a reachable refusal or the short-of-band-depth `Ok` path, not per-pixel-independent chance, which would essentially never hit any of them), not one fixed example; the property generator was confirmed strong enough by temporarily reintroducing the cycle-1 panic locally and watching it fail immediately (story 2.3, AC1) | covered | `tools/defs-build/src/propose.rs` -- `an_opaque_pixel_exactly_on_the_bands_top_row_is_seen_by_the_band_scan`, `a_fully_transparent_sprite_is_flagged_never_a_silent_0x0_or_1x1`, `a_sprite_whose_lower_band_alone_is_fully_transparent_is_flagged`, `a_gap_of_a_whole_tile_or_more_below_the_only_opaque_row_is_flagged_never_a_panic_or_a_silent_1x1`, `a_gap_smaller_than_one_tile_at_the_bottom_edge_still_proposes_1x1`, `a_gap_of_exactly_one_whole_tile_at_the_bottom_is_still_refused`, `a_gap_inside_the_band_stops_the_contiguous_depth_count`, `propose_never_panics_always_ok_or_a_named_err` (proptest), `turning_a_pixel_opaque_never_shrinks_the_proposal` (proptest) |
| **AC2**: the tileset's dominant 16x32-class prop (one cell of floor, two cells of screen height) proposes depth 1, never inflated by its own screen height; a real 112x64 vehicle proposes an exact multi-cell depth (never merely `>= 2`), because depth greater than one is common, not exceptional (story 2.3, AC2) | covered | `tools/defs-build/src/propose.rs` -- `the_16x32_lamp_shape_proposes_exactly_1x1`, `a_wide_fully_opaque_sprite_proposes_a_multi_cell_deep_footprint`; `tools/defs-build/tests/propose_real_art_acceptance.rs` -- `the_real_112x64_vehicle_proposes_a_depth_of_exactly_4_cells` |
| Height invariance (AC2's own mechanism), stated exactly as narrowly as it actually holds: for a sprite no wider than it is tall (the band is then capped by width, never by height), adding any number of rows above the lower band, transparent or fully opaque and with a randomised alpha pattern, never changes the proposal, up to the full `MAX_FOOTPRINT_CELLS` square -- proven as a property over many generated cases, not one example. For a sprite as wide as or wider than it is tall (a vehicle), the whole height is the band by design, so added height legitimately does change depth (AC2's own "depth greater than one is common"); the inverse is pinned too, so the band formula can never silently start claiming the wider guarantee it does not have (story 2.3) | covered | `tools/defs-build/src/propose.rs` -- `height_invariance_holds_for_a_sprite_no_wider_than_it_is_tall` (proptest), `a_sprite_as_wide_as_it_is_tall_does_have_its_depth_inflated_by_added_height` |
| **AC3**: an `[[object]]` may name an `archetype` instead of declaring its own `height`/`collider`; each field has exactly one source (both or neither is a build error, and an archetype supplying neither is rejected too), and an agent's correction is recorded in `defs/`, never in the proposer's own source. The one archetype-collider misfit only detectable once a real object's own `width` is known (an inset that does not fit a narrow object) is reported at the object's own `archetype = "..."` line, naming both, never at a line inside `defs/archetypes/`. A real object migrated to an archetype (the lamppost's own `pole`, a full-cell blocker, an underfoot flat) changes `defs_version` and nothing else in either generated artefact (story 2.3, AC3) | covered | `tools/defs-build/src/validate.rs` -- `lower_object`, `an_object_via_archetype_produces_the_same_object_def_as_the_explicit_equivalent`, `an_object_naming_an_unknown_archetype_is_named` (`tools/defs-build/tests/failure_fixtures.rs` -- also `an_archetype_supplying_neither_height_nor_collider_inset_is_named`, `an_archetype_collider_that_does_not_fit_a_narrow_objects_width_is_named_at_the_objects_own_archetype_line`); `defs/archetypes/city.toml`; real `cargo run --bin defs-build` output diffed against the previously committed artefacts (`defs_version` only) |
| The proposer never grows a per-object override table -- no committed object key or tileset sheet filename ever appears as a string literal in its own source, mechanically guarded, never merely by convention (story 2.3, AC3) | covered | `scripts/ci/check-proposer-no-content-keys.sh`, self-tested by `scripts/ci/tests/test-check-proposer-no-content-keys.sh` |
| **AC4**: no footprint is ever inferred at runtime -- `archetype` and the proposer never reach either generated artefact or a runtime's own source (`client/src/`, `server/`), and the proposer module is referenced by no file under `tools/defs-build/src/` but its own two and the tests (`validate.rs`/`emit.rs`/`atlas/*` included, not `lib.rs` alone); `defs-build`'s normal build path never reads sprite alpha to decide a footprint (swapping an object sheet's own alpha for a same-size different pattern leaves both artefacts' footprint/collider data byte-identical); the proposer writes stdout only, running it twice over the same real sheets is byte-identical, and running it at all leaves `defs/` byte-for-byte untouched (story 2.3, AC4) | covered | `scripts/ci/check-no-runtime-footprint-inference.sh`, self-tested by `scripts/ci/tests/test-check-no-runtime-footprint-inference.sh`; `tools/defs-build/tests/build_never_reads_alpha_for_footprint.rs`; `tools/defs-build/tests/propose_binary_edge.rs` -- `running_the_binary_leaves_the_defs_tree_untouched`, `running_the_binary_twice_over_the_same_real_sheets_is_byte_identical` |
| The proposer's own tile size constant is pinned against the committed `defs/balance/render.toml`'s own `render.tile_size_px`, so the two can never silently drift (story 2.3) | covered | `tools/defs-build/tests/propose_tile_size_pinned.rs` |
| The proposer never panics over any committed object's own sprite *rect* (cropped from its sheet, never the whole sheet) -- report-only: a named refusal on a real prop with a whole-tile-or-larger gap under its own art would be reported, not failed (none of the 11 committed objects hit one today, since a gap smaller than one tile is tolerated), and never asserted equal to the object's own authored footprint, which is not its authority (story 2.3) | covered | `tools/defs-build/tests/propose_report_only.rs` |
| **AC1**: an agent extending `defs/` runs one command, `scripts/dev/verify-defs.sh`, no arguments, no server toolchain beyond `cargo`, no `spacetime`, no network. Its exit-code mapping (0 every case passed, 1 a named content failure, 2 the harness itself could not run) is proven against a stubbed `cargo` standing in for the real one -- no CI job invokes the script itself, so the real end-to-end run is exercised only by hand (story 2.12, NFR27) | covered | `scripts/ci/tests/test-verify-defs.sh` |
| **AC2**: every committed rule key is named by at least one worked (`expect: pass`) and one deliberately failing (`expect: fail`) `.grid` file under `server/sim/tests/rule-examples/`; a key missing either turns the completeness test red by name, and a `rules:` header naming an uncommitted key does too (story 2.12, NFR27) | covered | `server/sim/tests/rule_examples.rs` -- `every_committed_rule_has_at_least_one_pass_and_one_fail_example` |
| **AC3**: a rule that breaks is reported against that specific rule (exact-set assertion against the declared `violations:` block, never "contains"), and the harness's own panic on a mismatch names the fixture path and the exact new-case path an agent adds the regression at (story 2.12, NFR27) | covered | `server/sim/tests/rule_examples.rs` -- `every_rule_example_matches_its_declared_violations` |
| The one command stays fast enough that an agent keeps running it: end to end (regenerate `defs/`, build the harness, run the corpus) well under ten seconds on a warm cache (story 2.12, NFR27) | deferred | nothing asserts this -- an in-test timing assertion is flaky by nature (Tim's direction) and no CI job runs the script for a wall-clock gate to attach to; `scripts/dev/verify-defs.sh` prints its own elapsed seconds so a regression is visible to a human running it, which is an observation, not coverage |
| **AC1**: the contact sheet renders every object's footprint and collider overlaid on its own sprite, grouped by archetype so an outlier is visible by comparison -- the projection from declared `width`/`height`/`collider`/`interact_at` to overlay rectangles is proven table-driven (a 1x1 prop, a max-footprint prop, a prop with no collider, an off-origin collider, a tall overhang, a tile size that does not divide sub-cells evenly), and the grouping/ordering function is a total order (archetype key, `None` -- bespoke -- sorted first, footprint size then object key within a group), never `HashMap` iteration order; two mutation-style guards (changing a declared `width`, separately a `collider`) prove the renderer is not decorative; the four per-layer toggles are pinned structurally (every toggle input is a document-order sibling of `<main>`, never nested inside the legend, so the CSS sibling combinator they depend on actually matches) and every class that selector names is proven to be emitted; against the real, committed `defs/` tree, every real object's own key appears in the sheet exactly once and every sprite rect the sheet draws lies inside its own named atlas page (story 2.5) | covered | `tools/defs-build/src/contact_sheet.rs` -- `a_1x1_prop_with_a_collider_projects_footprint_and_collider_flush_with_the_sprite`, `a_max_footprint_prop_projects_the_full_eight_by_eight_footprint`, `a_prop_with_no_collider_projects_none_never_a_zero_rect`, `a_collider_that_does_not_start_at_the_footprint_origin_keeps_its_own_offset`, `a_tall_overhanging_sprite_projects_the_footprint_at_the_bottom_and_an_overhang_band_above`, `interact_at_extending_beyond_the_footprint_keeps_its_own_negative_and_overspilling_bounds`, `a_tile_size_that_does_not_divide_subcells_evenly_is_never_assumed_one_subcell_one_pixel`, `objects_naming_no_archetype_land_in_their_own_bucket_sorted_first`, `within_a_group_cards_sort_by_footprint_size_then_key_never_insertion_order`, `an_object_naming_no_archetype_is_never_silently_dropped`, `changing_a_declared_width_changes_the_emitted_footprint_width_attribute`, `changing_a_declared_collider_changes_the_emitted_collider_rect_never_the_sprite_bitmap`, `the_four_layer_toggles_are_structural_siblings_of_main_never_nested_in_the_legend`, `every_layer_class_toggle_style_names_is_actually_emitted_by_render_card`; `tools/defs-build/tests/contact_sheet_fixture_golden.rs` (a byte-exact golden of the whole emitted sheet over a small fixture tree); `tools/defs-build/tests/contact_sheet_real_defs.rs` -- `every_real_objects_key_appears_in_the_sheet_exactly_once`, `every_sprite_rect_the_sheet_draws_lies_inside_its_own_named_atlas_page` |
| **AC2**: a reviewer identifies a prop whose footprint is plausible but wrong (a bench a player should walk behind, marked walk-through) and corrects it in `defs/` -- this is a human judgement act, not something a test can stand in for. What automation owns instead: the sheet exists, is current (AC3, below), is complete (every real object's key appears exactly once) and is legible by construction (every prop grouped with its archetype peers, carrying its own footprint/collider/`interact_at` overlay, a positive walk-through marker rather than blank ink, and its own `defs/objects/<file>.toml:<line>` to jump to) -- never a fake test standing in for the identification itself. One known case is named rather than left to be re-discovered: `defs/README.md` records `bridge_deck` as placeholder art (story 2.5) | covered | completeness: `tools/defs-build/tests/contact_sheet_real_defs.rs` -- `every_real_objects_key_appears_in_the_sheet_exactly_once`; legibility: `tools/defs-build/src/contact_sheet.rs` -- `a_walk_through_prop_is_marked_positively_never_by_blank_ink`, `tools/defs-build/tests/contact_sheet_fixture_golden.rs` -- `objects_naming_no_archetype_render_in_their_own_visible_bucket`; the identification of a wrong-but-plausible footprint itself is a manual review step this artefact enables, not an automated one |
| **AC3**: the contact sheet is regenerated on every `defs/` change, by the same `cargo run --bin defs-build` that already writes `defs.rs`/`defs.json`/the id manifest -- no second binary, no manual step. `scripts/ci/check-defs-current.sh` diffs the committed `tools/defs-build/contact-sheet.html` against a fresh regen exactly like the other three outputs, and restores it (and them) on any non-success exit so a local run never leaves the tree mutated. The header's own manifest fingerprint is asserted by value (the exact `sha256_hex` of this same build's own id manifest), and two different manifest inputs are proven to produce two different emitted headers, never a hardcoded or frozen placeholder (story 2.5) | covered | `scripts/ci/tests/test-check-defs-current.sh` -- "a stale contact sheet (nothing else drifted) fails", "a missing contact sheet fails before any regen"; `tools/defs-build/tests/contact_sheet_fixture_golden.rs` -- `the_contact_sheet_is_byte_identical_across_two_builds`; `tools/defs-build/tests/contact_sheet_real_defs.rs` -- `the_header_names_defs_version_and_the_exact_manifest_hash`; `tools/defs-build/src/contact_sheet.rs` -- `two_different_manifest_hashes_produce_two_different_emitted_headers` |
| The contact sheet never reads a pixel to decide where a footprint is -- it draws only the declared geometry `emit_rust`/`emit_json` already carry, referencing the already-committed atlas pages by relative path (never compositing, never a base64 re-encode); every asset URL it names resolves to a real file on disk relative to its own committed location (story 2.5) | covered | `scripts/ci/check-no-runtime-footprint-inference.sh`, self-tested by `scripts/ci/tests/test-check-no-runtime-footprint-inference.sh`; `tools/defs-build/tests/contact_sheet_real_defs.rs` -- `every_asset_url_resolves_to_a_real_committed_file_relative_to_the_sheets_own_location` |
| The input-collection pipeline a real `defs/` tree needs (sheet paths/dims/bytes, the codes golden) is shared, by one function (`build_from_repo_root`), between `bin/defs-build.rs`'s own edge and this crate's own tests over the real tree -- a step added to one is never silently skipped by the other (story 2.5) | covered | `tools/defs-build/src/lib.rs` -- `build_from_repo_root`, exercised by `tools/defs-build/tests/contact_sheet_real_defs.rs` and `tools/defs-build/src/bin/defs-build.rs` |
| The generation design document (`docs/generation.md`) is the named home for placement constraints, adjacency tables, density curves and neighbourhood parameter ranges; every committed `defs/rules/*.toml` key is recorded there, before or with the code, under the `## ` section matching its own constraint kind, and a `planned` row left unflipped once its key is committed fails the build by name; a machine-read section holds one table and nothing else, so a stray blank line can never silently drop rows; `## Must never be seen`'s own `Status` is derived from `Claimed by` and checked, never trusted; `docs/architecture.md` is asserted to name the document too (FR111, story 3.1) | covered | `server/sim/tests/rule_examples.rs` -- `every_committed_rule_has_a_current_row_in_the_generation_document`; `server/sim/tests/support/generation_doc.rs`'s own `check_rules_current` and `check_catalogue` branch tests (`check_rules_current_flags_a_missing_row`, `check_rules_current_flags_a_planned_row_whose_key_is_committed`, `check_rules_current_flags_a_row_under_the_wrong_section`, `check_rules_current_flags_an_orphan_committed_row`, `check_rules_current_allows_a_planned_row_with_no_committed_key`, `check_rules_current_flags_a_key_present_under_more_than_one_section`, `check_catalogue_flags_a_claimed_by_key_that_does_not_exist`, `check_catalogue_flags_a_claimed_by_key_under_the_wrong_section`, `check_catalogue_flags_status_claimed_with_an_empty_claimed_by`, `check_catalogue_flags_status_unclaimed_when_it_should_be_claimed`, `check_catalogue_flags_status_claimed_when_the_claiming_key_is_still_planned`) and its parser's own malformed-input tests (duplicate/repeated heading, stray heading, a row after a table's own blank-line end, unrecognised `pass`/`scope`/`reads`, among others) |

## Generation

Story 3.2: FR110's first two passes (land use, the street network) and
NFR8's periphery-reads-as-character requirement. Same Guard-path
discipline as the sections above.

| Requirement | Status | Guard |
| --- | --- | --- |
| FR110 pass 1 (land use) and pass 2 (street network) exist, are pure, and are total over any `u64` seed (never panic, every coarse cell assigned, at least one block produced) | covered | `server/sim/src/generation/land_use.rs`, `server/sim/src/generation/streets.rs`, `server/sim/tests/invariants.rs` -- `inv_generation_total_never_panics`, `server/sim/tests/generation_perf.rs`, `server/sim/tests/generation_golden.rs` |
| FR110 passes 3-7 (plot subdivision, building envelope, building type, interior layout, prop placement) | deferred | stories 3.3 on, one pass per story; `docs/generation.md`'s own `## Passes` section reserves each pass's own heading and pass id already |
| AC1 (totality: all four land uses present, no unassigned cell), AC2 (connectivity: no stranded region, judged by real per-cell footprint not a bounding box), AC3 (no dead end away from the site boundary, Manhattan-detour ratio bounded) -- each over arbitrary seeds, never a fixed sweep | covered | `server/sim/tests/invariants.rs` -- `inv_generation_all_four_land_uses_present`, `inv_generation_streets_connected_and_not_stranded`, `inv_generation_no_dead_ends_away_from_boundary`, `inv_generation_detour_ratio_bounded` |
| AC4/"not a perfect grid": block width and height each take at least `min_distinct_block_sizes` distinct values, both junction kinds are present, and at least two street classes are present -- over arbitrary seeds, each threshold a balance key rather than a literal | covered | `server/sim/tests/invariants.rs` -- `inv_generation_not_a_perfect_grid` |
| Exact tiling: every site cell is covered by exactly one block or by at least one street, and no two blocks overlap | covered | `server/sim/tests/invariants.rs` -- `inv_generation_exact_tiling` |
| Industrial never touches commercial, and residential reads as the majority land use by area (Artie's/Quentin's direction, cycle 1) | covered | `server/sim/tests/invariants.rs` -- `inv_generation_industrial_never_touches_commercial`, `inv_generation_residential_is_the_largest_land_use_by_area` |
| NFR8's land-use/street half -- the density falloff is non-increasing ring by ring from the field's own (off-centre) peak and strictly lower at the site's own edge than at the peak, the outermost ring is still fully assigned (no unassigned cell), and every arterial line starts at its own near site edge with no gap, at most one per city stopping short of the far edge (the T-termination) | covered | `server/sim/src/generation/land_use.rs` -- `ring_averages_are_non_increasing_and_strictly_lower_at_the_edge`, `density_peak_is_never_the_geometric_centre_across_a_seed_sweep`; `server/sim/tests/invariants.rs` -- `inv_generation_arterials_are_contiguous` |
| NFR8's block-size half -- peripheral blocks read as visibly larger than central ones, measured by density bands (what `target_block_size` itself reads), not distance from the peak. Two guards, deliberately different strengths (Quentin's direction, cycle 4: a weak per-city floor alone cannot tell a healthy city from a density-blind one): a per-city anti-inversion floor over arbitrary seeds (low-band mean at least `peripheral_low_band_floor_percent` of the high-band's, true for 4,997 of 5,000 measured seeds outright and within single-digit percent for the rest -- real split-jitter noise, not an inversion), and a pooled, deterministic guard over a fixed seed range that actually fails on a density-blind generator (summed low-band mean over summed high-band mean at least `peripheral_pooled_min_ratio_percent`, measured pooled ratio 241%). Artie's own harder 2x bar is judged on the three committed evidence seeds specifically, where it holds for two of three (2.6x each; the third, seed 2, reaches 1.67x -- disclosed, not silently missed, in that test's own doc comment) | covered | `server/sim/tests/invariants.rs` -- `inv_generation_peripheral_blocks_are_not_degenerate`, `peripheral_blocks_pooled_ratio_exceeds_a_density_blind_floor`; `server/sim/src/generation/streets.rs` -- `peripheral_blocks_are_at_least_1_6x_central_ones_on_the_evidence_seeds` |
| NFR8's prop/tile half -- density visibly thinning as fewer, sparser props per screen toward the periphery, and the periphery reading as three visually distinct edges rather than a uniform ring | deferred | prop placement (FR110's seventh pass) is the pass that reads density into a prop cadence; nothing places a prop yet |
| Generation is deterministic: the same seed always yields the same plan, byte-identical, across two derivations in one process and across pass 2 run twice over one pass-1 output; `GENERATION_VERSION` is bumped whenever the algorithm or seeding (never the balance data) moves a fixed seed's output, pinned against a frozen config local to the golden test, not live `defs::BALANCE` | covered | `server/sim/tests/generation_golden.rs`, `scripts/ci/check-golden-version-bump.sh`'s `generation_*` arm |
| Every generation-pass balance key is read once into `GenerationConfig`, never looked up by a pass or held as a literal, and every cross-key inconsistency a single key's own range cannot express (an odd street width, shares not summing to 100, a site extent not a multiple of the coarse cell size, a non-positive coarse cell size, `max_leaf_cells` under twice `min_leaf_cells`, `density_peak_offset_min_pct` over its own max, an arterial count's own `_min` over its `_max`, `max_block_depth_min_cells` over its own max, `p99_detour_percent` over `max_detour_percent`, `max_detour_excess_cells` over its own structural ceiling `2*block_size_max_cells + 2*arterial_width_cells`, `peripheral_low_band_floor_percent` over `peripheral_pooled_min_ratio_percent`) is refused with a named error | covered | `server/sim/src/generation/mod.rs` -- `GenerationConfig::from_balance`, its own `from_balance_rejects_*` tests |
| Two junctions on the same street either coincide (a true 4-way) or are refused outright -- never merely kept apart: `resolve_junction_position` returns no position unless it is clean against the registry, and the caller keeps the rect as a leaf rather than create a known-defective junction, including every arterial-arterial crossing (registered before any superblock's own recursion starts). Asserted at zero over arbitrary seeds, not a measured ceiling (Tim's direction, cycle 2) | covered | `server/sim/tests/invariants.rs` -- `inv_generation_no_staggered_junctions` |
| Every checker function (`reachable_from_first_node`, `dead_end_nodes`, `stranded_regions`, `junction_mix`, `detour_samples`, `build_graph`) has been seen to fail on a hand-built fixture, not only to pass on generated output | covered | `server/sim/src/generation/streets.rs` -- `build_graph` unit tests (`build_graph_splits_a_vertical_at_a_perpendicular_t_junction`, `build_graph_splits_both_streets_at_a_crossing`, `build_graph_keeps_collinear_abutting_segments_as_two_edges_sharing_one_node`, `build_graph_deduplicates_an_exact_duplicate_segment`) and `StreetNetwork::test_fixture`-based fixture tests (`a_maze_fails_dead_ends_and_detour`, `a_uniform_grid_fails_block_variance_and_has_no_three_way_junctions`, `two_disjoint_components_fail_connectivity`, `a_region_with_no_street_is_reported_stranded`) |
| The block-variety, street-tier-presence and periphery-area metrics are each `sim` functions returning findings, not re-implemented inline per test -- the same code the proptests call is what a hand-built fixture proves can fail | covered | `server/sim/src/generation/streets.rs` -- `StreetNetwork::distinct_block_sizes`, `StreetNetwork::street_classes_present`, `StreetNetwork::mean_area_by_density_band` (`inv_generation_peripheral_blocks_are_not_degenerate`'s own metric) and `StreetNetwork::mean_area_split_by_peak_distance` (the evidence-seed test's own metric), each exercised by both a proptest/fixed test and a hand-built fixture proving it can fail (`mean_area_by_density_band_reports_parity_for_a_uniform_grid`, `a_uniform_grid_fails_block_variance_and_has_no_three_way_junctions`) |
| The percentile arithmetic (`p99_ratio_pct`) and the density-monotonicity of `target_block_size`/`target_block_depth` are each pinned directly, not only inferred from generated output | covered | `server/sim/src/generation/streets.rs` -- `p99_ratio_pct_of_an_empty_slice_is_zero`, `p99_ratio_pct_of_one_sample_is_that_sample`, `p99_ratio_pct_of_exactly_100_samples_is_the_99th`, `p99_ratio_pct_of_101_samples_is_the_99th`, `target_block_size_is_monotone_non_increasing_in_density`, `target_block_depth_is_monotone_non_increasing_in_density` |
| The staggered-junction checker (`close_same_street_junction_pairs`) has been seen to both fire and stay quiet -- two close T-junctions on one street reported, a true coincident 4-way and a pair with a real net gap both left silent | covered | `server/sim/src/generation/streets.rs` -- `close_same_street_junction_pairs_reports_two_close_t_junctions_on_one_street`, `close_same_street_junction_pairs_does_not_report_a_true_coincident_four_way`, `close_same_street_junction_pairs_does_not_report_a_pair_just_over_the_separation` |
| The per-city periphery floor (`inv_generation_peripheral_blocks_are_not_degenerate`, above) is too weak on its own to fail a density-blind generator, so a separate, deterministic guard is pooled over a fixed seed range and checked against its own balance key; the guard's own hand-built fixtures prove `mean_area_by_density_band` reports both parity and a real difference, never only one of the two (Quentin's direction, cycle 4) | covered | `server/sim/tests/invariants.rs` -- `peripheral_blocks_pooled_ratio_exceeds_a_density_blind_floor`; `server/sim/src/generation/streets.rs` -- `mean_area_by_density_band_reports_parity_for_a_uniform_grid`, `mean_area_by_density_band_reports_a_real_difference_not_only_parity` |
| The evidence Artie reviews (`docs/generation/land-use-seed-*.svg`, `docs/generation/street-network-seed-*.svg`, three seeds each, one land use per block on the street SVG never per coarse cell) can never drift from the code that produced it | covered | `server/bounds/tests/generation_evidence_current.rs`, `server/bounds/src/generation_evidence.rs` |
| The Manhattan-estimator assumption 3.11's pathfinding story relies on -- an additive ceiling on every sampled pair's own overshoot, a ratio ceiling only for pairs `detour_long_pair_cells` apart or more (where a ratio, not an absolute budget, is the meaningful bound), and the 99th-percentile ratio separately, so the estimator's own contract is never just "the single worst pair stayed under a number" | covered | `server/sim/tests/invariants.rs` -- `inv_generation_detour_ratio_bounded`, `inv_generation_p99_detour_ratio_bounded` (own rows above); the precondition is guarded here, not re-derived by 3.11 |
| Land use and the street network are decoupled (no boundary snapping): a block's own land use is decided once, after subdivision, by majority coarse-cell area, so a district boundary only ever falls at a real block edge; `subdivide` forces a split whenever the current rect still spans more than one land-use region, which is what keeps every region touching a street without that snapping | covered | `server/sim/src/generation/mod.rs` -- `block_land_use`; `server/sim/src/generation/streets.rs` -- `spans_multiple_regions`; `server/sim/tests/invariants.rs` -- `inv_generation_streets_connected_and_not_stranded` |
| Institutional is several small, mutually non-adjacent pockets -- at least `institutional_min_pockets`, none over `institutional_max_pocket_share_percent` of the site -- never one contiguous slab (Artie's direction, cycles 2-3) | covered | `server/sim/tests/invariants.rs` -- `inv_generation_institutional_pockets_are_small` |
| "Each institutional pocket touches an arterial" (Artie's own further ask) is not enforced -- pass 1 authors the land-use field before pass 2 lays a single street (FR110's own ordering), so nothing in `land_use.rs` can know where an arterial will fall | deferred | a genuine architectural boundary this story does not close, not a story to schedule against |

Story 3.3: FR110 passes 3-4 (plot subdivision, the building envelope) and
FR115 (a building's footprint sized for what it needs to hold). Same
Guard-path discipline as the sections above.

| Requirement | Status | Guard |
| --- | --- | --- |
| FR110 pass 3 (plot subdivision) and pass 4 (the building envelope) exist, are pure, and are total over any `u64` seed (never panic; every block yields at least one plot; every seed either produces a plan or a typed `GenerationError`) | covered | `server/sim/src/generation/plots.rs`, `server/sim/src/generation/envelopes.rs`, `server/sim/tests/invariants.rs` -- `inv_generation_all_four_passes_never_panic`, `server/sim/tests/generation_perf.rs`, `server/sim/tests/generation_golden.rs` |
| AC1 (every plot fronts a street or is explicitly `open`; plots are disjoint, inside their own block, and a block's own area minus its plots' summed area is never negative) | covered | `server/sim/tests/invariants.rs` -- `inv_generation_every_plot_fronts_a_street`, `inv_generation_plots_tile_their_block` |
| AC2/FR115 (a footprint is never below its own land use's minimum usable interior, checked against the interior net; always inside its own plot; rejection is a typed, counted outcome, never a shrunk footprint) | covered | `server/sim/tests/invariants.rs` -- `inv_generation_no_envelope_below_its_class_minimum`, `inv_generation_envelope_inside_its_own_plot`; `server/sim/src/generation/envelopes.rs` -- the `a_plot_far_below_the_class_minimum_on_*`/`a_plot_exactly_at_the_class_minimum_*` unit tests |
| AC3 (footprint size stays within its own class's `[min, max]` band per envelope, a district shows at least `min_distinct_sizes` distinct sizes so a city of identical boxes cannot satisfy a mean band alone, and the pooled mean over seeds `0..256` matches the committed band +- tolerance) | covered | `server/sim/tests/invariants.rs` -- `inv_generation_envelope_sizes_are_varied`, `inv_generation_envelope_mean_size_matches_the_committed_band` |
| AC4 (building count fails generation outside `[min, max]`, derived from `target_count_per_million_cells` scaled by real site area and `count_tolerance_percent`; the rejection rate itself is bounded separately, so a generator that rejects half the district cannot pass by accident) | covered | `server/sim/tests/invariants.rs` -- `inv_generation_building_count_within_tolerance`, `inv_generation_envelope_rejection_rate_bounded`; `server/sim/src/generation/envelopes.rs` -- `a_deliberately_starved_config_makes_run_fail_the_building_count_tolerance_low`, `a_deliberately_bloated_config_makes_run_fail_the_building_count_tolerance_high` |
| A block's own street-abutting sides (`generation::block_sides`) are pass 2's own knowledge, read by pass 3 in O(1) per block rather than re-derived by scanning `StreetNetwork::edges` | covered | `server/sim/src/generation/streets.rs` -- `block_sides_reports_every_side_on_the_interior_as_street_abutting`, `block_sides_reports_a_side_on_the_site_boundary_as_not_abutting`, `block_sides_reports_no_frontage_at_all_for_a_block_spanning_the_whole_site` |
| The frontage checker (`PlotMap::landlocked_plots`) has been seen to both fire (an interior plot, a corner-touch-only plot, a plot straddling the block edge, a frontage one cell under the minimum, a plot fronting a non-abutting side) and stay quiet (a real fronting plot) -- never only ever exercised against real generator output | covered | `server/sim/src/generation/plots.rs` -- `landlocked_plots_flags_an_interior_plot_with_no_boundary_edge`, `landlocked_plots_flags_a_corner_touch_only_plot`, `landlocked_plots_flags_a_frontage_one_cell_under_the_minimum`, `landlocked_plots_accepts_a_plot_straddling_the_block_edge_as_landlocked_too`, `landlocked_plots_flags_a_plot_fronting_a_side_that_does_not_abut_a_street`, `landlocked_plots_does_not_report_a_real_fronting_plot` |
| A rhythm of 2-3 module widths per block face, not a fresh random width per plot (a terrace, Artie's direction): covers the row length exactly with no gap or overlap, and shows real repetition within one face | covered | `server/sim/src/generation/plots.rs` -- `rhythm_plots_covers_the_row_exactly_with_no_gap_and_no_overlap`, `rhythm_plots_shows_repetition_within_one_face` |
| Plots need not tile their own block (Tim's own simpler contract): a deep block's own core, left after every abutting face has cut its own row, is simply unplotted remainder, never modelled as a `Plot`/yard type -- `PlotMap::remainder_cells` is the explicit account, never a silent gap | covered | `server/sim/src/generation/plots.rs` -- `remainder_cells_accounts_for_a_deep_blocks_own_unplotted_core` |
| A block with no street frontage at all (a sliver block, Artie's own named case) gets one explicit whole-block `open` plot, never a landlocked or missing one | covered | `server/sim/src/generation/plots.rs` -- `a_sliver_block_with_no_frontage_gets_one_whole_block_open_plot` |
| Envelopes are structurally disjoint without an `O(envelopes^2)` scan: every envelope is contained in its own plot, and plots are themselves mutually disjoint (within one block directly, across blocks because blocks are themselves disjoint) -- proven as a structural argument, pinned against a hand-built fixture rather than only ever inferred from real generator output (Quentin's own cost warning: a pairwise scan across a district's own envelope count is exactly the shape that made `test`'s own proptest budget expensive in 3.2) | covered | `server/sim/tests/invariants.rs` -- `inv_generation_envelope_inside_its_own_plot`'s own doc comment states the argument; `envelope_footprints_from_two_different_plots_never_overlap` pins it against a hand-built fixture |
| The `footprint_sized_for_interior_usability` row (`docs/generation.md`) stays `planned`, not flipped to `committed` -- Tim's direction (this story does not extend the rule engine, no new `defs/rules/*.toml` row backs it) is followed over Artie's own request to commit it, since `server/sim/tests/rule_examples.rs`'s `check_rules_current` fails the build on a `committed` row with no matching rule key (an orphan row); FR115 itself is enforced here by the generator plus the invariants above, not by a rule-engine row | deferred | flagged in this story's own PR for Tim/Artie to reconcile next cycle, not silently decided by picking a side without saying so |
| The evidence Artie reviews (`docs/generation/plots-seed-*.svg`, `docs/generation/envelopes-seed-*.svg`, three seeds each, `open` plots hatched, front edge a heavier stroke, envelopes a darker opaque fill over a lighter yard tint, two viewport-scale insets on the envelope SVG) can never drift from the code that produced it | covered | `server/bounds/tests/generation_evidence_current.rs`, `server/bounds/src/generation_evidence.rs` |
| Generation is still deterministic across all four passes: the same seed always yields the same plan, byte-identical, across two derivations in one process and across pass 2 run twice over one pass-1 output; `GENERATION_VERSION` bumped to 2 and `generation_v1.golden` retired in favour of one `generation_v2.golden` covering every implemented pass (Tim's direction: no two goldens side by side) | covered | `server/sim/tests/generation_golden.rs`, `scripts/ci/check-golden-version-bump.sh`'s `generation_*` arm |
| Every new plot/envelope balance key is read once into `GenerationConfig` (never a literal in `plots.rs`/`envelopes.rs`), and every cross-key inconsistency a single key's own range cannot express (a plot width min over its own max, an envelope minimum the plot minimum can never hold, an envelope minimum over its own class max, a mean width/depth outside its own class band, an odd `side_gap_periphery_cells`) is refused with a named error | covered | `server/sim/src/generation/mod.rs` -- `GenerationConfig::from_balance`, its own `from_balance_rejects_*` tests |

## World addressing

Story 1.5: `(x, y, floor, layer)` addressing, collision, floor transitions
and cell ownership (FR117-FR119). Same Guard-path discipline as the
sections above.

| Requirement | Status | Guard |
| --- | --- | --- |
| Two cells at the same (x, y) on different floors both exist without conflict; an entity on floor 0 tests collision only against floor 0 (FR117) | covered | `server/sim/tests/world_acceptance.rs` -- `two_cells_at_the_same_xy_on_different_floors_never_conflict` |
| A player under a bridge deck passes under it the whole span; the same walk one floor up interacts with the deck geometry above (FR117) | covered | `server/sim/tests/world_acceptance.rs` -- `walking_under_the_bridge_is_unobstructed_the_whole_span` and `walking_on_the_bridge_deck_interacts_with_its_railings` |
| Entering a transition cell changes the entity floor and its collision set together, from one function application (FR117) | covered | `server/sim/tests/world_acceptance.rs` -- `entering_a_transition_yields_the_target_floor_and_its_collision_set_together` |
| A door is an ordinary walkable cell; walking through one is a continuous position sequence with no space transition (FR118) | covered | `server/sim/tests/world_acceptance.rs` -- `walking_through_a_doorway_is_a_continuous_walk_with_no_space_transition` |
| A building ownership id is queryable for the player position, inside and outside it (FR119) | covered | `server/sim/tests/world_acceptance.rs` -- `the_buildings_id_is_queryable_inside_and_not_outside` |
| The canonical fixture world and its hand-typed query answers are checked against the real `sim::world` query functions (Quentin direction) | covered | `server/sim/tests/world_conformance.rs` |
| The shared conformance fixture (`fixtures/world-conformance.v1.json`) is never stale against the `sim::world::fixture` source | covered | `server/bounds/tests/world_fixture_current.rs` |
| The `chunk_key` packing round-trips and never panics, including at the extremes (FR145) | covered | `server/sim/tests/world_chunk.rs` |
| A collision query costs a bounded, small number of dense-storage accesses regardless of world size, and the collision grid stays within its documented 1-bit-per-cell budget | covered | `server/sim/tests/world_perf.rs` |
| An ownership query scans only the areas sharing the queried chunk, never every area in the world (FR119, FR120, FR122) | covered | `server/sim/tests/world_perf.rs` -- `ownership_lookup_scans_only_the_queried_chunk_not_every_area_in_the_world` |
| The client-side TypeScript port of chunk addressing and whole-cell collision reads the same fixtures/world-conformance.v1.json its Rust oracle reads, and agrees on every case (NFR30, story 1.8) | covered | `client/tests/unit/world/conformance.test.ts` |
| The client-side TypeScript port of floor transitions and building/room ownership reads the same fixtures/world-conformance.v1.json its Rust oracle reads, and agrees on every case (NFR30, story 1.7) | covered | `client/tests/unit/world/conformance.test.ts`, `client/tests/unit/world/ownership.test.ts`, `client/tests/unit/world/transitions.test.ts` |
| A `placed_object` row's own `layer` column is checked against its def's `layer` at `WorldSpec::build` time (and the client's `world-spec.ts` mirror), so the two can never diverge (Tim's direction, story 2.2) | deferred | the world-generation story that first ingests `placed_object` rows into `WorldSpec`/`checkWorldSpec` -- no `PlacedObjectSpec` exists yet (`sim::codes::layer`'s own doc comment: "there is no live call site yet that constructs a `PlacedObject` at all") |
| **AC4** (story 2.4, FR128): a doorway cell must leave a passable gap of at least `movement.player_body_width_subcells` by `_height_subcells` (never a literal) -- a pure function over a sub-cell walkability grid, never a hand-laid map type, proven by ASCII-art unit fixtures (exact width passes, one sub-cell narrower fails, an offset gap at a cell boundary is handled, the verdict follows a changed balance value), an exact hand-derived `Finding` over a real wall-ring/lamppost block, and a hard `Err` (never a silent empty report) for a seed the body itself does not fit at | covered | `server/sim/src/world/walkability.rs` -- `a_gap_of_exactly_the_body_width_erodes_to_one_passable_cell`, `a_gap_one_sub_cell_narrower_than_the_body_erodes_to_nothing`, `a_gap_offset_by_one_sub_cell_at_a_cell_boundary_is_handled`, `changing_the_balance_value_changes_the_verdict`, `narrow_passages_errors_when_the_body_does_not_fit_at_the_seed`, `a_passage_too_narrow_for_the_body_is_reported_even_though_raw_reachable`; `server/sim/tests/world_walkability.rs` -- `a_lamppost_narrowing_the_door_below_body_width_cuts_off_the_interior`; `inv_doorway_gap_at_least_body_width_is_one_component_under_erosion` |
| **AC5** (story 2.4, FR128): a walkable region enclosed with no door is unreachable, reported over a hand-laid test block built from real `defs` objects -- every enclosed region is reported (not just the first), deterministically sorted by smallest sub-cell, an iterative flood fill (never recursive, so a generated district never blows the stack); a component touching any of the window's own four edges is never reported (it continues past the window), judged on the sub-cells a `narrow_passages` origin's own body actually covers so all four edges (not just north/west) are exempted correctly, so a district run can be chunked; an out-of-bounds or blocked seed is a hard `Err`, never a silent empty report | covered | `server/sim/src/world/walkability.rs` -- `a_fully_open_grid_reports_no_enclosed_region`, `a_room_with_no_door_is_reported`, `the_same_room_with_a_door_is_not_reported`, `two_separate_sealed_rooms_are_both_reported`, `edge_touching_components_are_never_reported_but_a_true_sealed_room_still_is`, `a_passage_cut_off_flush_against_the_east_edge_is_never_reported`, `an_out_of_bounds_seed_is_an_error_not_an_empty_report`, `a_blocked_seed_is_an_error_not_an_empty_report`; `server/sim/tests/world_walkability.rs` -- `a_closed_ring_reports_the_interior_as_enclosed`; `inv_sealed_ring_yields_exactly_one_enclosed_region`; `inv_removing_a_door_never_reduces_enclosed_regions`; `inv_ring_open_to_any_window_edge_is_never_reported` |
| **AC5**'s other half: "the same check will later run over generated districts without modification" -- `rasterise`/`enclosed_regions`/`narrow_passages` take a walkability grid and bounds, never anything about how the arrangement was laid out, so a generator can call them unchanged as a post-condition on what it places (story 2.4, FR128) | deferred | the Epic 3 generator story -- no generator exists yet; `server/sim/tests/world_walkability.rs`'s own test block is the only caller today, and it is a hand-laid arrangement, not a generated one |
| A cell collision-style query (`enclosed_regions`/`erode`/`narrow_passages`) over a generated-district-sized walkability grid finishes within a generous time ceiling, `--release` -- a regression guard against a quadratic blowup, never a benchmark (story 2.4) | covered | `server/sim/tests/world_walkability_perf.rs` |

## Rendering

Story 1.6: FR123-FR127's depth sort, rank ladder and decomposition. Story
1.7: FR120-FR122's enclosure visibility (near-side retraction, window
translucency, subway/street floor culling). Same Guard-path discipline as
the sections above.

| Requirement | Status | Guard |
| --- | --- | --- |
| The rank ladder tens are unique among live codes, and every pool rank is a multiple of ten (FR123) | covered | `server/sim/tests/codes.rs` -- `layer_ranks_are_unique_and_pool_ranks_are_multiples_of_ten` |
| A deprecated layer code (`overhead`) stays seeded but refuses a live rank lookup, on both sides (Tim direction) | covered | `server/sim/tests/codes.rs` -- `deprecated_layer_codes_stay_seeded_but_refuse_live_rank`, `client/tests/unit/render/layer-ranks.test.ts` |
| A committed `sim::codes` layer line never changes or disappears once merged; only new lines may be appended (NFR36) | covered | `scripts/ci/check-codes-append-only.sh` |
| The one client-side rank-ladder mirror (`layer-table.ts`) never drifts from the golden-pinned server mapping on any code, name, rank or deprecated flag (Quentin/Tim direction, cycle 2) | covered | `scripts/ci/check-layer-table-current.sh` |
| The comparator is the sole ordering authority: a pool container keeps `sortableChildren` at `false`, and the children of a real Pixi `Container` end up in the comparator order regardless of the order they were attached in, including across repeated re-sorts with no duplication | covered | `client/tests/unit/render/pixi-order.test.ts` |
| The FR123 sort-key unit (tile * `SORT_SUBDIVISIONS`) is declared exactly once, round-trips exactly, is strictly monotonic, and resolves sub-tile movement to a different unit before a whole tile is crossed (Tim direction, cycle 2) | covered | `client/tests/unit/render/sort-units.test.ts` |
| The FR124 floor offset is zero at floor 0, proportional and strictly monotonic in floor (higher floor draws further up the screen), and sourced from the `render.storey_height_px` balance key rather than a literal (Quentin direction, cycle 2) | covered | `client/tests/unit/render/screen-position.test.ts` |
| The tile size and storey height are `defs/balance/render.toml` keys, not TypeScript literals, and fold into `defs_version` | covered | `scripts/ci/check-defs-current.sh`, `scripts/ci/check-defs-version-bump.sh` |
| A real, mounted Pixi display list produces the same order the comparator produces over the identical, committed fixture scene, both at rest and after a real keyboard-driven move (Quentin direction) | covered | `client/tests/e2e/test-street.spec.ts` (which replaced `render-order.spec.ts`) against `client/tests/unit/test-street/drawables.test.ts` and its shared goldens (`golden.ts`, generated by `regen-golden.ts`, never hand-typed) |
| The demo player movement is resolved by `world/movement.ts` swept-AABB `step` against real fixture colliders, and the scene only re-sorts when the player own quantised sort-key position actually changes (replacing the old bounds-clamped `player-step.ts`) | covered | `client/tests/unit/world/movement.test.ts`, `client/tests/unit/test-street/drawables.test.ts` |
| A per-cell sub-rect is only legal on whole-tile boundaries; a prop whose declared footprint does not match the real pixel dimensions of its art throws at mount rather than drawing a stretched or fractional slice (Artie/Tim direction, cycle 2) | covered | `client/src/test-street/scene.ts` -- `sliceTexture`/`sliceAlongAxis` |
| Every sprite the street scene mounts is drawn within the canvas -- an off-canvas scene fails loudly instead of passing every id-based check silently (Artie direction, cycle 2) | covered | `client/src/test-street/scene.ts` -- `assertSpritesWithinCanvas`, called at mount |
| The client-only trace-matrix check (no cargo) and the full run agree, and `client-check` runs the former so a client-only PR is never gated on the slowest half of the graph (Tim direction, cycle 2) | covered | `.github/workflows/ci.yml` -- the `client-check` job's own step, `scripts/ci/check-trace-matrix.sh --client-only` |
| The render-order e2e hook never ships in the production bundle, same as every other `window.__bc` use | covered | `.github/workflows/ci.yml` -- the `client-build` job's own bundle grep |
| Walking speed is a named constant derived from `movement.walk_speed_millicells_per_s`, never a literal scattered through movement code; crossing a 40-cell viewport at the committed speed takes 18s within tolerance across jittered frame deltas (FR137) | covered | `client/tests/unit/world/no-speed-literal.test.ts`, `client/tests/unit/world/defs-movement.test.ts` |
| Client movement code under `world/**` never imports `pixi.js`, values from `net/` (only `net/bindings` types, type-only) or `demo/`, and never touches `window`/`document` | covered | `client/biome.json` -- the `src/world/**` override's `noRestrictedImports` and `noRestrictedGlobals`, run by the `client-check` job |
| A collision query is `O(1)`: the number of cells examined for the same move is identical whether the grid holds 10 or 100k objects placed elsewhere, and no row query happens in the movement path | covered | `client/tests/unit/world/movement.test.ts` -- the counting-wrapper test |
| Colliders declared in `defs/objects` reach the running client: the grid def map is built from the fetched document, and a def sub-tile collider both blocks and lets the player past the free part of its cell (FR128) | covered | `client/src/world/object-defs.ts`, `client/tests/unit/world/defs-movement.test.ts` |
| The grid frees a chunk when its last entry is deleted, and a floor when its last chunk goes, so a streaming session never accumulates chunks it no longer has an entry in | covered | `client/tests/unit/world/collision-grid.test.ts` -- `inv_collision_grid_matches_rebuild` compares `allocatedChunkCount` too |
| The player can never walk off the drawn world: for any input sequence, every step keeps the drawn body over the interior floor or the pavement | covered | `client/tests/unit/test-street/drawables.test.ts` -- the boundary-ring property |
| Holding a direction key moves the avatar within three animation frames with no round trip (FR137); it comes to rest against a real collider and never pauses at a collider-less prop | covered | `client/tests/e2e/test-street.spec.ts` (which replaced `movement.spec.ts`) |
| FR120: a near-side wall of the building the viewer occupies is retracted, keyed on the ownership id alone (never proximity); a viewer outside any building or a wall it does not own is never retracted. Near-side-ness itself is derived from ownership and cell coordinates alone (the cell directly south is not the same building), never a hand-authored per-prop flag | covered | `client/src/render/visibility.ts` -- `isRetracted`, `isNearSideWall`, `client/tests/unit/render/visibility.test.ts` |
| FR121: a window drawable that is not hidden is translucent, at `render.window_alpha` (a balance key, never a literal); retraction wins over the window rule for a retracted near-side window | covered | `client/src/render/visibility.ts` -- `computeVisibility`, `client/tests/unit/render/visibility.test.ts` |
| FR122: two floors of opposite sign are never co-visible, compared by sign alone (never against the literal -1); a floor transition changes floor and position together, from one call, never one without the other | covered | `client/src/render/visibility.ts` -- `isFloorCulled`, `client/src/world/floor-walk.ts` -- `stepAndTransition`, `client/tests/unit/world/floor-walk.test.ts` |
| A floor transition fires only when a step walks into its anchor cell, never on arrival by transition and never while a key stays held on the landing cell; two mutually-targeting transitions never bounce (FR117/FR122) | covered | `client/src/world/floor-walk.ts`, `client/tests/unit/world/floor-walk.test.ts` |
| The transition index refuses a duplicate anchor rather than silently keeping only the last one declared at it | covered | `client/src/world/transitions.ts`, `client/tests/unit/world/transitions.test.ts` |
| The player own drawable `floor` follows a floor transition, so the player is never floor-culled from its own new position the instant it lands | covered | `client/src/render/sort-key.ts` -- `setDrawableFloor`, `client/src/test-street/drawables.ts` -- `updatePlayerDrawable`, `client/tests/unit/test-street/drawables.test.ts` |
| While the viewer is inside an enclosure, that same building floors above the viewer own floor are culled too (Artie direction, the same ownership-keyed rule as retraction) | covered | `client/src/render/visibility.ts` -- `isStoreyAboveCulled`, `client/tests/unit/render/visibility.test.ts` |
| The atlas page reader (`render/atlas-pages.ts`'s `AtlasPageLoader`) caches a page by filename and a cropped texture by object id, evicts a rejected load from whichever cache it failed in so the next demand retries, and names the object when its `atlas.page` indexes no real page (story 2.6, Artie's direction cycle 1) | covered | `client/tests/unit/render/atlas-pages.test.ts` |
| The atlas reader has a real caller: the shop counter draws through `AtlasPageLoader.objectTexture` instead of a `ModernTileset/` URL import, and the mounted street's own distinct-bound-page count is a real assertion, not a deferred row (story 2.6, Tim's direction cycle 1) | covered | `client/src/test-street/scene.ts` -- `textures.set("counter", await atlasPageLoader.objectTexture(...))`; `client/tests/e2e/test-street.spec.ts` -- the `distinctBoundAtlasPages` assertion |
| No masking, filter or render-texture construct exists anywhere under `client/src/` (FR121) | covered | `scripts/ci/check-no-masks.sh`, run by the `client-check` job; `scripts/ci/tests/test-check-no-masks.sh`, run by `scripts-tests`; `client/tests/e2e/enclosure.spec.ts` -- `masksAllNull`, checked against the real, mounted display list |
| The permanent `render/**` modules (`visibility.ts` included) cannot import `pixi.js`, same discipline as the `world/**` import ban | covered | `client/biome.json` -- the `src/render/**` override's `noRestrictedImports` |
| The real, mounted `VisibilityApplier` reaches the same FR120/FR121/FR122 states the pure `computeVisibility` function predicts, after a real keyboard-driven walk into and out of an enclosure and into and out of the subway | covered | `client/tests/e2e/enclosure.spec.ts` |
| The street own committed transitions never target another transition own anchor cell -- a fixture-data sanity check, not the guard against the underlying bounce bug (see the edge-triggered `floor-walk.ts` row above) | covered | `client/tests/unit/test-street/fixture.test.ts` |
| Two storeys whose screen rects overlap (a bridge deck over the street it spans) are separated by one four-pass stack per floor, drawn in ascending floor order -- never a z-offset, a container trick in scene code, or a floor term in the sort key (FR124) | covered | `client/src/render/floor-stacks.ts`, `client/tests/unit/render/floor-stacks.test.ts`, `client/tests/e2e/test-street.spec.ts` -- the mounted order at the underpass |
| The test street's own hand-picked `assetKey`/`sourceCol`/`sourceRow` retire for a prop that has a real `object_def`, in favour of its packed `atlas` rect (Tim's direction, story 2.2) | partial | the shop counter draws through `render/atlas-pages.ts`'s `AtlasPageLoader` (its def's own sprite is the only one byte-for-byte the street already drew, story 2.6); the remaining rows (`trash_bin`, `lamppost`, `shop_window`, `wall_segment`, `foot_stairs`, `bridge_deck`) need Artie's own art-mapping fixes and a `sprite` repeat-axis defs-model change neither packer story can decide -- Story 2.13, issue #297 |

## The hand-laid test street

Story 1.13: the one scene every foundation in Epic 1 is proven on, by use
rather than by unit test (AC1-AC3). Same Guard-path discipline as the
sections above.

| Requirement | Status | Guard |
| --- | --- | --- |
| AC1: the street is hand-laid from a hardcoded object set, placed by real `defs/objects` ids, with no generator -- and is one directory plus one import in `main.ts` to delete when Epic 3 replaces it | covered | `client/src/test-street/fixture.ts`, `client/tests/unit/test-street/street-conformance.test.ts`; `client/biome.json` -- the `render/**`, `world/**` and `input/**` overrides that forbid importing it back |
| AC2: the street really contains every foundational case -- two floors at one (x, y), a transition cell on each floor walked, a building with retractable near-side walls, a window with furniture behind it, a multi-cell prop, and a prop whose collider is smaller than its footprint | covered | `client/tests/unit/test-street/street-conformance.test.ts` |
| The street is legal world data: no overlapping same-kind ownership rects, every rect inside the one chunk its anchor corner names, both ends of every transition standable -- the same refusals `sim::world::WorldSpec::build` makes (NFR30) | covered | `client/src/world/world-spec.ts`, `client/tests/unit/world/world-spec.test.ts`, `client/tests/unit/test-street/street-conformance.test.ts` |
| The scripted walk is collision-feasible against the real resolver and the real grid before any browser runs it, and a whole lap ends back where it started | covered | `client/tests/unit/test-street/street-conformance.test.ts` |
| AC3: pressing a movement key drives a character down that street, with collision, depth sorting, wall retraction, floor culling and appearance all behaving correctly together rather than only in isolation -- real key presses only, no teleport hook, every wait a condition and never a fixed timeout | covered | `client/tests/e2e/test-street.spec.ts` |
| A pixel-level regression at the interior checkpoint and the underpass is caught even when every id-based assertion above still passes -- a fixed 1920x1080 viewport, animations disabled, a frozen street crowd for a deterministic frame, baselines generated on the CI image (Quentin's direction) | covered | `client/tests/e2e/test-street.spec.ts` -- the two `toHaveScreenshot` checks, `.github/workflows/update-visual-baselines.yml` |
| NFR2: a walked session at 1080p, including interior transitions, keeps p95 frame *work* under 8 ms and max under 16.7 ms, and does not leak over ten minutes. Partial: the harness and the regression gate ship here; "a busy street at rush hour" needs NPC crowds and is deferred to story 5.4 (L3 frame budget). The sustained-60-FPS half is deliberately not gated in CI -- a GPU-less headless runner cannot measure it without being flaky -- and belongs to spike 1.14 | partial | `client/tests/e2e/street-perf.spec.ts` (the `perf` Playwright project), `.github/workflows/ci.yml` -- the `e2e` job's own perf step, `.github/workflows/perf-soak.yml` -- the ten-minute soak, intentionally outside the `ci` gate |

## Input and intents

Story 1.9: FR148's intents and FR149's keybindings. Same Guard-path
discipline as the sections above.

| Requirement | Status | Guard |
| --- | --- | --- |
| Nothing in the input layer encodes what an intent means (AC3): an `Intent` is an object instance and its definition, with no verb, action or kind field, and `src/input/**` cannot import a procedure, an interaction module, `net/` (bindings included), `demo/` or `pixi.js` | covered | `client/src/input/intent.ts`; `client/biome.json` -- the `src/input/**` override; `scripts/ci/check-input-boundary.sh`, run by `client-check`; `scripts/ci/tests/test-check-input-boundary.sh`, run by `scripts-tests`; `client/tests/unit/input/pick.test.ts` -- the exact-shape and two-consumers tests |
| An object declares an interaction by carrying an `interact_at` rect and no other way; the rect has positive area, reaches no further than `INTERACT_AT_MAX_REACH_CELLS` beyond its own footprint, and never lies entirely inside the object own collider -- rejected identically by the build tool and by the client own parser (FR148) | covered | `tools/defs-build/src/validate.rs` -- `check_object_interact_at`, `client/src/defs/parse.ts` -- `checkInteractAtReach`, `tools/defs-build/tests/shared_malformed_cases.rs`, `client/tests/unit/defs/malformed.test.ts` |
| Picking never uses PixiJS hit-testing: exactly one `pointerdown`/`pointermove` listener on the canvas element, and no `eventMode`, `interactive` or per-sprite pointer handler anywhere under `client/src/` | covered | `client/src/input/pointer.ts`, `client/tests/unit/test-street/no-federated-events.test.ts` |
| A click or hover resolves to the object *drawn* under the pointer, including the part of a bottom-anchored sprite drawn above its own footprint: the index covers the cells a prop art overhangs into, and the point must fall inside the rect that object own sprites occupy. Known limitation, accepted for this story: the test is the whole sprite rectangle, transparent corners included -- per-pixel alpha is not consulted, so a click in a transparent corner of a sprite still resolves to it (FR148) | covered | `client/src/input/pick.ts` -- `topmostAt`'s narrow phase, `client/tests/unit/input/pick.test.ts` -- the drawn-extent cases, `client/tests/e2e/intents.spec.ts` -- the bin-lid click |
| The affordance follows the player, not only the mouse: with the pointer held still, walking into an object own `interact_at` marks it and walking out clears it, as does a visibility change under a still cursor (FR173) | covered | `client/src/input/pointer.ts` -- `refresh`, `client/tests/unit/input/pointer.test.ts`, `client/tests/e2e/intents.spec.ts` |
| A click is visibly ignored without anything being drawn for it: the highlight stays withheld and the cursor blips to `not-allowed` once, never pulsing, and an in-reach click adds no confirmation effect of its own (AC2, D17) | covered | `client/src/input/pointer.ts` -- `IGNORED_CURSOR_MS`, `client/tests/unit/input/pointer.test.ts`, `client/tests/e2e/intents.spec.ts` -- the real canvas element's own cursor |
| The affordance mark is confined to the hovered object own drawables and leaves nothing behind: one additive overlay sprite per drawable, built on hover and destroyed on un-hover, never a tile tint, an outline or a filter (FR173, FR121) | covered | `client/src/render/highlight.ts` -- `highlightOverlayAlpha`/`highlightOverlaySpec`, `client/src/render/pixi-highlight.ts` -- `HighlightApplier`, `client/tests/unit/render/highlight.test.ts`, `client/tests/unit/render/pixi-highlight.test.ts`, `scripts/ci/check-no-masks.sh` |
| The full FR173 grammar is exhaustive at unit level over the truth table hovered×in-reach / hovered×out-of-reach / unhovered×in-reach / unhovered×out-of-reach: a reachable-but-unhovered object gets no treatment and no cursor change, a hovered-but-unreachable one gets the `pointer` cursor and zero overlay specs, never a dimmed variant; `highlightOverlayAlpha` is monotone non-decreasing in strength, proportional to source alpha, never exceeds `ceiling*sourceAlpha`, and stays within [0,1] for any strength in [0,100] and for junk outside it (FR173) | covered | `client/src/render/highlight.ts`, `client/tests/unit/render/highlight.test.ts`, `client/tests/unit/input/pointer.test.ts` |
| The overlay tracks its source every frame it is alive, never a snapshot frozen at hover start: position, scale, anchor, texture and visibility are all mirrored, and a source that goes invisible mid-hover loses its overlay in the same frame; a re-sort re-attaches the same overlay instances rather than rebuilding them, allocating nothing on the one hot path a player crossing sort units while an object is marked walks every step; the refresh ticker subscribes only while something is marked and never double-subscribes across a hammered hover sequence; a hammered sequence of hover/un-hover cycles, strength changes and a visibility change interleaved ends at the same container child count before and after the final `set(undefined)`; `setHighlight` with an unchanged id is a genuine no-op -- no teardown, no rebuild, no callback (FR173, NFR2) | covered | `client/src/render/pixi-highlight.ts` -- `HighlightApplier`, `client/tests/unit/render/pixi-highlight.test.ts` |
| The mark never survives its context, and the options menu opening does not falsely claim to (Derek's direction): a window blur or the tab hiding clear the mark and the cursor and latch against a scene-driven `refresh()` relighting it while unfocused, and `focus`/visibility-restored re-resolves at the last known pointer position, the symmetric restore. The options menu opening leaves an already-true mark exactly as it is (the panel does not make the pointer's own position or the player's own reach any less true) and only ignores *new* pointer events until it closes, when a real move (or a hover that started while it was open) resolves again (FR173) | covered | `client/src/input/pointer.ts` -- `suspend`/`resume`, `client/tests/unit/input/pointer.test.ts`, `client/src/main.ts` -- the options menu's own `onOpenChange` wiring |
| The U1 dial stays `[20, 100]`, default 60, pinned by a unit test rather than only a comment; a slider drag applies live through `onDisplayPreview` before it commits, and `HighlightApplier.setStrength` never duplicates or rebuilds the live overlay; the stored value survives a reload, and a garbage or wrong-version payload falls back to 60 in memory without writing; only the four Pixi-native blend modes are ever assigned anywhere under `client/src/`, and no import from `pixi.js/advanced-blend-modes` exists (FR173) | covered | `client/src/settings/display-settings.ts`, `client/tests/unit/settings/display-settings.test.ts`, `client/src/ui/options-menu.ts`, `client/tests/unit/ui/options-menu.test.ts`, `scripts/ci/check-no-masks.sh`, `scripts/ci/tests/test-check-no-masks.sh` |
| A real pixel changed: a hovered, in-reach object's frame differs from its own un-hovered baseline by at least half of what the same comparison shows at the dial's own maximum (a floor derived from the object's own behaviour, never a magic pixel count), every differing pixel lies inside that object's own drawn rect (nothing bleeds onto its tile, its neighbours or the ground pass), the pointer leaving returns the frame to the baseline within zero tolerance, and a hovered-but-unreachable frame is pixel-identical to the baseline (FR173) | covered | `client/tests/e2e/test-street.spec.ts` |
| A real click on the real canvas records exactly one intent for the object clicked, records an ignore and no intent once out of range, and a rebind survives a reload while cleared storage boots on defaults with no console error (AC1/AC2/AC4) | covered | `client/tests/e2e/intents.spec.ts` |
| Movement keys resolve `KeyboardEvent.code`, never `.key`, so Shift, CapsLock and a non-QWERTY layout never stop a walk; the old key stops working the moment it is rebound | covered | `client/src/input/keyboard.ts`, `client/tests/unit/input/keyboard.test.ts` |
| Keybindings live in exactly one versioned `localStorage` key, written only on a real rebind; reading never throws and never writes, and never touches another key in the same origin (FR149) | covered | `client/src/input/keybindings-storage.ts`, `client/tests/unit/input/keybindings-storage.test.ts` |
| The DOM UI surface is an exact allowlist of exactly three surfaces -- the FR151 options menu, the connection notice and (deferred to story 4.6) the boot name prompt -- and nothing else: every top-level element a surface mounts carries `data-bc-surface` with an allowed value, no missing and no unknown, checked against `document.body`'s own children on a real page | covered | `client/src/ui/options-menu.ts`, `client/src/ui/connection-notice.ts`, `client/tests/e2e/connection-notice.spec.ts` |
| The options menu opens and closes with `Escape`, which takes the keyboard while open, never pauses the world, and never blocks with a native dialog; it has exactly the sections Audio, Display, Controls, in that order, as stacked headings, and every control has a real consumer or a persisted value a named later story reads | covered | `client/src/ui/options-menu.ts`, `client/tests/unit/ui/options-menu.test.ts` |
| The options menu is usable without a mouse: focus starts inside the panel on open and returns to the page on close, Tab cycles within the panel rather than leaking behind the backdrop, Tab cancels a capture instead of binding itself, and every keycap prints a readable key name rather than a `KeyboardEvent.code` | covered | `client/src/ui/options-menu.ts` -- `trapTab`/`keycapLabel`, `client/tests/unit/ui/options-menu.test.ts` |
| NFR6: mouse and keyboard only -- no touch, pointer-gesture or controller handling is written anywhere in the input layer | covered | `client/src/input/pointer.ts` -- `pointerdown`/`pointermove` only, no touch or gesture listener; `client/src/input/keyboard.ts` |
| FR150/FR152: all UI is DOM except transient, object-bound canvas container views (there is no fiction object yet, so no canvas text exists anywhere under `client/src/` today) -- enforced mechanically, not by convention | covered | `scripts/ci/check-no-canvas-ui.sh`, `scripts/ci/tests/test-check-no-canvas-ui.sh` |
| A connect error at boot and a drop after a successful connect both surface as `ConnectionStatus` `"disconnected"` (`net/connection-status.ts`'s plain string union, never an SDK type) and show "Connection lost"; a slow first connect shows "Connecting…"; neither ever claims a retry is happening, since this build does not retry. Debounced so a brief blip never flickers it; the notice never takes focus, never suspends the keyboard, never uses a native dialog, and is announced with `role="status"`/`aria-live`; reconnection is out of scope for this story, so once shown the notice stays up | covered | `client/src/net/connection-status.ts`, `client/src/net/connection.ts`, `client/src/ui/connection-notice.ts`, `client/tests/unit/connection.test.ts`, `client/tests/unit/ui/connection-notice.test.ts`, `client/tests/e2e/connection-notice.spec.ts` |
| NFR22: no HUD with net worth, balances, counters or objective markers -- the story 1.1 ping indicator (a fixed-position DOM readout with no requirement behind it) is deleted; `document` is banned in `render/**` (`client/biome.json`), so nothing there can create a DOM readout again | covered | `client/biome.json` -- the `src/render/**` `noRestrictedGlobals` override, `scripts/ci/check-no-canvas-ui.sh` |
| FR152: anything informational that belongs to the fiction (job boards, council notices, a certificate on the wall) is rendered in the world, not the DOM. This story adds no in-world information object at all -- its job is only to make sure there is nowhere else to put one (the row above) | deferred | | the job board (first fiction object) |

## Debug tooling

Story 1.12: FR165's overlays and FR168's production flag. Same Guard-path
discipline as the sections above.

| Requirement | Status | Guard |
| --- | --- | --- |
| FR168/AC1: the flag is not exposed in production at all -- the overlays are behind one dynamic import inside an `import.meta.env.DEV` branch, so Rollup emits no chunk for them and a production build has no code any input, query string or console call could activate. Proven three ways: structurally at source, by grepping the real built assets, and by driving every registered overlay's own activation against a real production build | covered | `scripts/ci/check-debug-boundary.sh`, `scripts/ci/tests/test-check-debug-boundary.sh`, `.github/workflows/ci.yml` -- `client-build`'s own debug-sentinel grep, `client/tests/e2e/deploy-smoke.spec.ts` -- the off-means-off test |
| Nothing but `main.ts` may reach `client/src/debug/`, and no second SVG surface may appear over the canvas anywhere under `client/src/` | covered | `client/biome.json` -- the `../debug/**` and `src/debug/**` overrides, `scripts/ci/check-debug-boundary.sh` |
| AC2: every collider rectangle is drawn at its true position, and three states are distinguishable -- a real collider, one declared with no area, and none at all. Decided in a pure builder and carried on `data-bc-collider`, so it is assertable rather than only visible. Each state is read from the source that can hold it: the live grid for a real collider, the definition for the other two, because a cell-aligned zero-area collider is in no cell of the grid at all and would otherwise draw nothing. The live street declares no zero-area collider (and this story does not loosen `defs/` to add one), so that state is proven at unit and jsdom level against the same interface and the same mount the page uses | covered | `client/src/debug/collision-rects.ts`, `client/tests/unit/debug/collision-rects.test.ts`, `client/tests/unit/debug/overlays.test.ts`, `client/tests/e2e/debug-overlays.spec.ts` |
| AC3: each drawable's computed sort key is legible where objects overlap, and the order shown is the one the renderer actually applied, never one the overlay worked out for itself | covered | `client/src/debug/sort-labels.ts`, `client/tests/unit/debug/sort-labels.test.ts` -- the stagger and round-trip tests, `client/tests/e2e/debug-overlays.spec.ts` -- every label checked against `window.__bc.renderOrder` |
| AC4: overlays are drawn in a deliberately non-diegetic style, checked rather than eyeballed -- one shared palette of saturated colours, hairline strokes and a UI monospace face, with every colour an overlay paints asserted to come from it | covered | `client/src/debug/debug-style.ts`, `client/src/debug/overlay-conformance.ts`, `client/tests/unit/debug/overlay-conformance.test.ts`, `client/tests/e2e/debug-overlays.spec.ts` -- the one supplementary screenshot |
| AC5: a later navmesh, chunk-boundary or citizen-route overlay registers with this framework rather than building its own, and inherits the whole contract (unique id, off by default, idempotent toggling, nothing left behind, palette-only colours) plus the production off-means-off check, with no new test file | covered | `client/src/debug/overlay-registry.ts`, `client/src/debug/overlay-conformance.ts`, `client/tests/unit/debug/overlay-conformance.test.ts` -- driven off `DEBUG_OVERLAYS` itself |
| An overlay reads the live collision grid the movement resolver reads, never a second expansion of `defs/`; its cost is bounded by the viewport rather than by how many objects the world holds; the viewport window itself is projection code in `render/`, tested there rather than in the composition root, and returns an empty window instead of an unbounded loop for a camera that describes no rectangle; and with every overlay disabled it does no work at all, so NFR2's frame budget is untouched | covered | `client/src/render/screen-position.ts` -- `visibleCellBounds`, `client/tests/unit/render/screen-position.test.ts`, `client/tests/unit/debug/collision-rects.test.ts` -- the live-grid and counting-wrapper tests, `client/tests/unit/debug/overlays.test.ts` -- the zero-reads-while-disabled test |

## Scheduled-reducer timing

Story 1.3: `docs/spikes/1.3-scheduled-reducer-timing.md`'s measured drift
budget and post-publish schedule rule (R10, D7). Same Guard-path
discipline as the sections above.

| Requirement | Status | Guard |
| --- | --- | --- |
| The spike report measured SpacetimeDB version never goes stale against the `server/Cargo.toml` pin, the `docs/architecture.md` stack line, or the pinned CLI installer exact patch | covered | `scripts/ci/check-spike-pin.sh` |
| The drift claims in `docs/spikes/1.3-scheduled-reducer-timing.md` are re-runnable with one command | covered | `scripts/dev/run-sched-timing-spike.sh` |
| Whether a pending scheduled row survives a schema-changing publish, or a publish to Maincloud, is unmeasured (only a same-wasm, same-schema local republish was measured) | deferred | a schema-changing publish exercised against Maincloud with a pending scheduled row already queued -- the deploy story (this one) only proves an *additive* publish round-trips (NFR33's own guard already forbids a non-additive one from ever reaching `deploy.yml`), so this stays unmeasured until a story adds a real scheduled reducer for it to matter to |

## Backup and restore

Story 1.4: `docs/spikes/1.4-backup-restore.md`'s logical export/restore and
the platform limitations it found. NFR39 split into two rows while no
Maincloud deploy workflow existed; the deploy story (this one) wires the
first into `.github/workflows/deploy.yml` and provisions the Maincloud
credential the second was blocked on (same Guard-path discipline as the
sections above).

| Requirement | Status | Guard |
| --- | --- | --- |
| The restore has been tested: export -> restore -> verify against a real SpacetimeDB instance, including adversarial values (every non-scheduled table, Timestamp-bearing ones included), a real `auto_inc` id gap (the restored sequence advancing past the recorded floor in the exported manifest, never merely to the maximum id present in the restored data, so no id the source ever issued is re-issued; an overshoot correctly aborting the whole call), byte-budgeted multi-batch restore, and the refusal paths (NFR39) | covered | `scripts/ci/check-backup-restore.sh` |
| Every non-scheduled table has a `restore_<table>` reducer -- a table nobody adds one for can never actually be restored | covered | `server/bounds/tests/restore_coverage.rs` |
| The world is backed up before every migration (NFR39) | covered | `.github/workflows/deploy.yml` -- the `backup` job, always run before `publish-module` (`needs:`); `scripts/ci/check-deploy-workflow.sh` asserts that dependency mechanically |
| An unattended scheduled export runs daily and alerts on its own failure, including a schedule GitHub silently disabled | deferred | `backup.yml`'s own daily `schedule` trigger and a dead-man's-switch that does not depend on that same schedule to notice it stopped firing -- neither exists yet; this story provisions the credential `backup.yml`'s header names as the blocker but does not itself add the schedule, so `backup.yml` stays `workflow_dispatch`-only |
| A full restore has been performed and verified against Maincloud (AC4), including a real, >=100,000-id auto_inc gap-fill under the reducer execution limits Maincloud itself imposes, with its wall time measured, not merely local timings extrapolated | deferred | the Maincloud credential this story adds a place for (`maincloud` environment secrets, `vars.MAINCLOUD_OWNER_IDENTITY`/`vars.BACKUP_DATABASE`) has not actually been provisioned yet -- `.github/workflows/backup.yml`'s `rehearsal` job (which already carves that gap with `scripts/ops/seed-id-gaps.sh` and logs the restore's own wall time) is correct by inspection and unexercised; moves to `covered` once a green `rehearsal: true` run is linked |
| Cross-table consistency during export (each table is its own transaction) | deferred | the first reducer that writes two tables in one transaction (e.g. `citizen` + `citizen_state`) -- nothing reminds anyone today because none does yet |
| No reducer other than `restore_<table>` writes an auto_inc table while a restore is open -- a client-facing write racing the gap-fill loop could observe or create an id the exported data still needs | deferred | the first story whose reducer accepts a live client connection *and* writes an auto_inc table; today a restore always targets a database name no client connects to, by procedure (`scripts/ops/restore-world.sh`'s own doc comment), not by a lock the module enforces |
| The spike report measured SpacetimeDB version never goes stale against the same three pins story 1.3 does | covered | `scripts/ci/check-spike-pin.sh` |

## Boot budget

Story 1.14: `docs/spikes/1.14-boot-budget.md`'s measured milestones and D4's
subscription-decode sweep. Same Guard-path discipline as the sections above.

| Requirement | Status | Guard |
| --- | --- | --- |
| NFR1: cold boot to player-controllable in under 1 second, measured on a mid-range laptop over a typical domestic connection. Partial: the harness, the milestones (permanent marks in `client/src/boot/boot-marks.ts`) and the D4 decode sweep ship here, re-runnable with one command; a PR-blocking timing gate does not exist yet -- 20 cold, throttled samples is too slow and too noisy for every PR -- and belongs to the story that builds the real boot path (FR144-146), once real crowd content exists to measure it against | partial | `client/tests/e2e/boot-budget/boot-budget.spec.ts` (the `boot` Playwright project), `scripts/dev/run-boot-budget-spike.sh`, `.github/workflows/ci.yml` -- `client-build`'s gzipped bundle size budget and `boot-smoke`'s end-to-end run, `client/tests/e2e/boot-marks.spec.ts`'s deterministic atlas request-count/byte gate, the cheap standing proxies for the two dominant terms until that story lands the real gate |
| The `docs/spikes/1.14-boot-budget.md` report measured SpacetimeDB version never goes stale against the server/Cargo.toml pin, the docs/architecture.md stack line and the pinned CLI installer | covered | `scripts/ci/check-spike-pin.sh` |
| The committed report `docs/spikes/1.14-boot-budget.md` is exactly the output of `scripts/dev/generate-boot-budget-report.mjs` over the committed raw JSON and run metadata -- never hand-transcribed | covered | `scripts/ci/check-boot-budget-report-current.sh` |
| The atlas term's regression gate is request-shaped (count and bytes before `player-controllable`), not a byte-sum over the whole public art catalogue -- so it still guards the dominant term docs/spikes/1.14-boot-budget.md measured even when the catalogue's own total size is unrelated to what one boot fetches | covered | `client/tests/e2e/boot-marks.spec.ts` |
| The boot-budget spike module (`server/spikes/boot_budget`) still compiles, and no spike code (its generated bindings, the decode-only HTML entry) reaches the production bundle | covered | `.github/workflows/ci.yml` -- `build`'s own compile step, `client-build`'s dist-leak check |
| The boot-budget harness itself (script, Playwright project, spike module) runs end to end on a Linux CI runner, not only the Windows dev box it was built on | covered | `.github/workflows/ci.yml` -- `boot-smoke` |

## Deploy

This story: publishing the module to Maincloud and the client to GitHub
Pages, with a backup before every publish (NFR39, above), a structural
guard against a destructive publish, and a real post-deploy smoke check.
Same Guard-path discipline as the sections above.

Cycle 1 (Quentin's direction): a row is only `covered` once it has actually run for real, not merely
once its guard exists -- the credential and variables this story adds a place for
(`vars.DEPLOY_ENABLED` included) are not provisioned yet, so nothing below has ever exercised the
live Maincloud/Pages path. `deferred`/`partial` rows below say exactly how far each one actually
got.

| Requirement | Status | Guard |
| --- | --- | --- |
| A build under the real Pages base (`/browser-city/`) never references any top-level dist entry root-absolute -- a misconfigured base fails the build, not the deploy | covered | `scripts/ci/check-pages-bundle.sh`, run on every client PR by `ci.yml`'s `client-build` job against a production-style build; the same guard runs again in `deploy-client` against the real build once `deploy.yml` is enabled |
| The production URI actually reaches the build: the `ws://127.0.0.1` local-dev fallback (`client/src/net/config.ts`) never ships in a Pages build, and the expected URI is actually present | covered | `scripts/ci/check-pages-bundle.sh` (same guard and PR-time caller as the row above) |
| A publish never carries a destructive command or flag (`--delete-data`/`-c`/`--clear-database`/`--break-clients`/`spacetime delete`), and every job that publishes `needs:` (and can only run after) the job that backs up first (NFR39) | covered | `scripts/ci/check-deploy-workflow.sh`, run directly against `deploy.yml` by `ci.yml`'s `scripts-tests` job, with its own fixture tests under `scripts/ci/tests` |
| The `backup` job's first-deploy exception positively recognises "database not found" rather than treating any failure as one | covered | `scripts/ops/check-database-exists.sh`, fixture-tested under `scripts/ops/tests` with a stubbed CLI (found/not-found/generic-error) |
| The deployed bundle serves the exact commit `deploy.yml` published, not a previous one Pages propagation lag would otherwise let a smoke check pass against | deferred | `.github/workflows/deploy.yml` -- the `deploy-client` job's own `<meta name="bc-build">` stamp and the `smoke` job's bounded poll for it exist and are structurally sound, but have never run for real: `vars.DEPLOY_ENABLED` is not set, so `deploy.yml` has never completed a live run |
| The deployed game actually reaches player-controllable: it loads with no failed request and no console/page error, its WebSocket dials the configured Maincloud URI and database, and its initial subscription applies | partial | `client/tests/e2e/deploy-smoke.spec.ts` (the `deploy-smoke` Playwright project) -- rehearsed for real by `ci.yml`'s `e2e` job against a production-base build under `/browser-city/`, backed by a disposable local SpacetimeDB, on every client PR; `deploy.yml`'s `smoke` job runs the identical spec against the live Maincloud/Pages path, but that path has never actually run (see the row above) |
| The client, once deployed, is redeployed only when it actually needs to be -- compared against what is live, never the previous commit | deferred | `.github/workflows/deploy.yml` -- the `changes` job's live-vs-resolved-SHA `git diff`; structurally sound, never exercised against a real live deploy |
| The post-deploy smoke check reuses one fixed identity rather than minting a fresh one every run (NFR39 live-database hygiene) | deferred | the first story whose `client_connected` or `connect` path actually writes a row (`server/src/lib.rs`'s `identity_connected` writes nothing today, so a fresh identity every smoke run pollutes nothing) -- when it lands, the fixed identity goes in through Playwright (`addInitScript`/storage) or a server-side smoke identity, never a URL query parameter (readable by any visitor, and captured verbatim by Playwright's own `trace`/`playwright-report` artifacts on a public repo) |

## The defs_version handshake

Story 2.8: FR147's `module_version` view, `boot/handshake.ts`'s pure decision, and `boot/
boot-gate.ts`'s ordering. Same Guard-path discipline as the sections above.

| Requirement | Status | Guard |
| --- | --- | --- |
| The module publishes `defs_version` and `protocol_version` (a SHA-256 over the generated TypeScript bindings) as a public anonymous view over two compiled-in constants, never a table -- no row to stamp, nothing to migrate (NFR33), no write on `client_connected` | covered | `server/src/version.rs`, `scripts/gen-protocol-version.sh`, `scripts/ci/check-protocol-version-agrees.sh` |
| A subscription already held open across a hot-swap republish is pushed the new row (a delete of the old one, an insert of the new), and a fresh subscription opened after the republish reads the new value too -- the production case, not a one-off query with no subscriber connected | covered | `scripts/ci/check-view-live-refresh.sh`, against `server/tests/fixtures/view_v1`/`view_v2` on a real disposable SpacetimeDB instance |
| The handshake's own `defs_version`/`protocol_version` ride the same initial subscription as `demo_ping` -- one subscribe message, no extra reducer or procedure call, on the matched-version path | covered | `client/src/net/connection.ts`, `client/tests/unit/connection.test.ts`, `client/tests/e2e/defs-handshake.spec.ts` -- the exactly-one-frame assertion |
| The decision -- both match is `proceed`; only `defs_version` differs is `refetch-defs`; `protocol_version` differs is a guarded `reload`, `updating` on a repeat for the same server version -- is one pure function, every branch table-tested, including a missing/empty/malformed remote version never reading as a match | covered | `client/src/boot/handshake.ts`, `client/tests/unit/boot/handshake.test.ts` |
| The client never draws a frame using a definition set it knows to be stale: the scene mount is structurally gated behind a `VerifiedDefs` only the boot gate can produce, the boot gate never resolves it before the handshake settles (or times out unreachable), and a mismatch arriving after mount stops the ticker before taking the reload/updating path -- there is no live defs swap and no scene remount once mounted. A `stopDrawing` that throws (no renderer yet) never itself prevents the reload/degrade that follows | covered | `client/src/boot/boot-gate.ts`, `client/src/boot/handshake-latch.ts`, `client/src/boot/post-mount-guard.ts`, `client/src/test-street/scene.ts` -- `MountStreetSceneOptions.defs: VerifiedDefs`, `client/tests/unit/boot/boot-gate.test.ts` -- the order-of-calls assertions, `client/tests/unit/boot/post-mount-guard.test.ts` -- a matching re-insert is a no-op, a differing one stops drawing before reloading, a late handshake after an unreachable mount is compared too, and a throwing `stopDrawing` still reaches reload/degrade, `client/tests/e2e/defs-handshake.spec.ts` -- the held-open-refetch, zero-frames proof |
| The gap between the boot gate settling and the post-mount guard being wired cannot silently drop a handshake, across *two* real async windows -- the gate's own internal awaits, and `Application.init()`'s own await, which has no renderer to stop drawing with yet: `HandshakeLatch.latest()` keeps the most recent version regardless of settlement, and `boot/boot-sequence.ts` replays it through the guard twice, synchronously around the caller's own `appInitPromise`, before ever returning anything to mount -- a version that changed during either window is caught before the first frame, not just a matching one that is a no-op | covered | `client/src/boot/handshake-latch.ts` -- `latest()`, `client/src/boot/boot-sequence.ts`, `client/src/main.ts` -- `setPostMountGuard` called with no `await` in between, a null-safe `stopDrawing`, `client/tests/unit/boot/handshake-latch.test.ts` -- the `latest()` suite, `client/tests/unit/boot/boot-sequence.test.ts` -- including the second-replay-around-appInitPromise suite |
| A stale `defs_version` still stale after the one cache-busted refetch attempt degrades to not-drawing with the FR151 notice immediately -- never a retry loop, since the identical URL would only ever be answered by the browser's own HTTP cache on a second attempt (NFR42) | covered | `client/src/boot/boot-gate.ts`, `client/tests/unit/boot/boot-gate.test.ts`, `client/tests/e2e/defs-handshake.spec.ts` -- the every-request-stays-stale case |
| A `protocol_version` mismatch (at boot or mid-session) reloads at most once per server version, guarded by a versioned `sessionStorage` key -- a storage write that does not land, or a throwing `reload()` itself, degrades to `updating` rather than falling through to an unguarded reload; a repeat for the same server version also settles on `updating` | covered | `client/src/boot/guarded-reload.ts`, `client/src/boot/reloaded-for-storage.ts` -- `writeReloadedFor`'s own boolean return, `client/src/ui/connection-notice.ts` -- the `"updating"` status, `client/tests/unit/boot/guarded-reload.test.ts`, `client/tests/unit/boot/reloaded-for-storage.test.ts`, `client/tests/unit/boot/boot-gate.test.ts`, `client/tests/unit/boot/post-mount-guard.test.ts`, `client/tests/unit/ui/connection-notice.test.ts` |
| An unreachable connection is "unknown", never "known stale": the boot gate mounts the already-fetched defs rather than refusing to render. A handshake that never settles at all (a hung socket) times out to unreachable rather than blocking the gate forever; a failed first `fetchDefs` is treated as an unknown local `defsVersion`, never a silent bypass of the handshake | covered | `client/src/boot/boot-gate.ts` -- `settleWithTimeout`, `client/tests/unit/boot/boot-gate.test.ts` -- the unreachable, timeout and failed-first-fetch cases |
| A rejected subscription does not hang the boot gate forever -- it settles as unreachable, the same as a connect error | covered | `client/src/net/connection.ts` -- `onError`, `client/tests/unit/connection.test.ts` |
| A `defs/**`-only change (not only `client/**`/`server/**`) re-runs the e2e job, since it changes handshake behaviour | covered | `.github/workflows/ci.yml` -- the `changes` job, `e2e` filter |
