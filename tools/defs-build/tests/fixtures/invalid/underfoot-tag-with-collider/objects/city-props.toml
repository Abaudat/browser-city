[[object]]
id = 1
key = "trash_bin"
name = "Trash Bin"
layer = "furniture"
sprite = { sheet = "fixtures/objects/trash-bin-test.png", x = 0, y = 0, w = 16, h = 16 }
width = 1
height = 1
collider = { x0 = 4, y0 = 4, x1 = 12, y1 = 12 }
# Contradictory metadata: a collider (it blocks) and the `underfoot` tag
# (it is explicitly walkable) can never both be true of the same object.
tags = ["underfoot"]
